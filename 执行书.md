# ChatGPT-Fix 分阶段执行书

> **供阶段执行代理使用：** 本书是 live runbook（实时执行手册）和 append-only receipt ledger（追加式回执账本）。每个阶段使用一个新 Codex 对话和一个 Goal；实现任务使用 `superpowers:subagent-driven-development`，依次完成实现、自审、规格审查、质量审查。

**执行目标：** 严格依照 [计划书.md](计划书.md) 从 P0 推进到 P8；任何阶段只执行本阶段授权范围，验收后创建本地 release 分支并 handoff 到新对话。

**当前状态：** P2 / VERIFYING / `work/p2-live-inspection` / 目标 `release/0.4.0`（2026-08-06 交接轮：P2 离线门禁与 live acceptance 全过，A3 已授予；P3 详细卡已追加；接受事务完成后进入 RELEASE-BRANCHED / HANDED-OFF，最终状态见冻结 handoff）。

---

## 1. 当前执行控制面

| 项 | 当前值 |
|---|---|
| 当前线程 | 当前 Codex P2 user-owned task；真实 thread ID 仅写 `%TEMP%` handoff，不进入 Git |
| 当前标题 | `ChatGPT修复｜P2实时只读发现` |
| 当前 Goal | 在不读取 process、`.codex` 或 package `LocalState`，且不改变任何 live 状态的条件下，让 Packer 成为 `OpenAI.Codex` 当前用户包的唯一 canonical `inspect`/`plan` 生成者；Manager 只解析、校验并原样展示 Packer 计划；形成 doctor v2 源码提案但不运行 doctor |
| P2 子仓库 | 独立 child worktree `d:\project\worktrees\ChatGPT-Fix\chatgpt-fix-p2-20260806`；canonical checkout 只用于接受后 release alignment |
| 当前起点 | `release/0.3.0@3e7750348739b71e6b2111bf2498a77139a484b8` |
| 当前工作分支 | `work/p2-live-inspection` |
| 目标版本 | `0.4.0` |
| 权威计划 | `$ChildRoot\计划书.md` |
| 本执行书 | `$ChildRoot\执行书.md` |
| 根项目 ID | `D:\project` |
| 发布状态 | local-only；无 remote/push/tag/PR |

当前授权：A0、A0q、A1、A2-P2 与 A10-NTC delegated evidence 登记范围已授予；A3 已由用户在 P2 收尾时明确授予（P3 baseline staging 写集）；A1n、广义 A2、A4a、A4b、A5-A7、A8a-A8c、A9 未授予。P2 只允许修改本卡 allowlist 内的 Rust workspace/source、schemas、fixtures、tests、probe script、doctor v2 proposal、build records/checksums、两书和 native token-cost delegated receipt，并写 task-local 构建输出、本地 commits/refs 及 `%TEMP%` acceptance journal/handoff；P2 的 live 读取仅限 A2-P2 精确 selector；不运行 doctor、不修改 shortcut/WindowsApps/配置、不启动或终止 ChatGPT、不读 `.codex`/process/LocalState。A0q 已扩展（2026-08-06）：允许以 `D:\project\.qoder\repowiki` 为辅助知识源，QoderCLI 使用 `Qwen3.8-Max` 与 Qoder DeepSeek 系列（`DeepSeek-V4-Flash`/`DeepSeek-V4-Pro`），Hermes 使用 DeepSeek 模型（`provider=deepseek`，本机默认 `deepseek-v4-flash`），Qoder/Hermes 只读旁路尽量经 squad 工具编排；仍禁工具、session persistence、文件/状态修改。

## 2. 阶段状态机

每个阶段只能按以下顺序推进：

```text
DRAFT
  -> AUTHORIZED
  -> ACTIVE
  -> VERIFYING
  -> ACCEPTED
  -> RELEASE-BRANCHED
  -> HANDED-OFF
```

- `DRAFT`：两书已定义阶段，但 Goal 尚未创建。
- `AUTHORIZED`：阶段所需授权已在对话和本书记录。
- `ACTIVE`：唯一 Goal 已创建，阶段工作分支已就绪。
- `VERIFYING`：实现停止，正在运行测试、审查、diff 和安全验证。
- `ACCEPTED`：所有验收通过，accepted commit 已确定。
- `RELEASE-BRANCHED`：`VERSION`/root manifest 一致，release branch 和 root ref 已建立。
- `HANDED-OFF`：临时 handoff 已冻结、下一项目任务已创建且绑定式 `HANDOFF_ACCEPTED` 已由预期 thread 验证，当前 Goal 才可标记 complete。

若遇到授权不足，阶段保留在 `DRAFT` 或 `AUTHORIZED`，不得创建会在执行途中撞上危险授权门的 Goal。若同一 blocker 连续三个 Goal turn 仍无法解决，才允许按 goal 规则标记 `blocked`。

## 3. Task shell 与通用阶段启动程序

P0 使用既有 canonical checkout，并显式定义：

```powershell
$CanonicalRoot = 'D:\project'
$CanonicalChild = 'D:\project\ChatGPT-Fix'
$TaskRoot = $CanonicalRoot
$ChildRoot = $CanonicalChild
```

P1 起每个 project-scoped worktree task 必须改为下列变量，所有实现、测试和 child Git 只使用 `$ChildRoot`：

```powershell
$TaskRoot = (git rev-parse --show-toplevel).Trim()
$ChildRoot = Join-Path $TaskRoot 'attached\ChatGPT-Fix'
$CanonicalRoot = 'D:\project'
$CanonicalChild = 'D:\project\ChatGPT-Fix'
```

`$CanonicalRoot` 只用于 accepted-version governance、root refs 和创建下一任务；`$CanonicalChild` 只允许在阶段 ACCEPTED 后执行 clean release checkout alignment（接受版本对齐），不得承载本阶段代码修改。P1+ 禁止把 `$CanonicalChild` 当成工作目录。

新阶段任务必须按序执行：

```powershell
Get-Content -Raw (Join-Path $TaskRoot 'workspace.yaml')
Get-Content -Raw (Join-Path $ChildRoot 'AGENTS.md')
Get-Content -Raw (Join-Path $ChildRoot '计划书.md')
Get-Content -Raw (Join-Path $ChildRoot '执行书.md')
python (Join-Path $TaskRoot 'repo-control\repo_control.py') doctor
git -C $ChildRoot status --short --branch
git -C $TaskRoot diff --cached --name-status
```

预期结果：目标项目仍注册为 `ChatGPT-Fix`；root 与 child index 为空；child 在本阶段工作分支；任何 root 既有未跟踪/未暂存内容原样保留。

项目 worktree 任务还必须验证：

```powershell
Get-Item $ChildRoot -Force
git -C $ChildRoot rev-parse --show-toplevel
git -C $ChildRoot status --short --branch
```

随后只回报 `HANDOFF_ACCEPTED` 并等待上一任务发送 `START_GOAL`。收到该消息后才调用 `create_goal` 创建执行书中该阶段唯一 Goal，再开始文件修改；不得与上一阶段 Goal 重叠。

## 4. 分支与版本程序

阶段开始：

```powershell
git -C $ChildRoot status --short --branch
git -C $ChildRoot rev-parse --abbrev-ref HEAD
```

阶段验收前：

```powershell
git -C $ChildRoot diff --check
git -C $ChildRoot status --short
git -C $TaskRoot diff --cached --name-status
```

阶段实现与审查通过后，按以下 fail-closed（失败关闭）事务接受版本。运行前必须由详细执行卡赋值 `$PhaseId`、`$JournalPath`、`$PhaseAllowedPaths`、`$OldVersion`、`$NewVersion`、`$CandidateMessage`、`$ReceiptMessage`、`$CurrentWorkBranch`、`$ExpectedCurrentWorkRootOid`、`$AllowCreateCurrentWorkRef`、`$ReleaseBranch`、`$NextWorkBranch`；P0 的实际值见第 6 节，P1+ 必须从冻结 handoff 取得 expected task-start OID并令 `$AllowCreateCurrentWorkRef=$false`。任何原生命令非零退出都立即抛错，不得继续读取旧 `HEAD`：

```powershell
$ErrorActionPreference = 'Stop'
Set-StrictMode -Version Latest
if ($PSVersionTable.PSVersion.Major -lt 7) { throw 'acceptance transaction requires PowerShell 7+' }

function Invoke-CheckedNative([string]$Label, [scriptblock]$Command) {
  $Output = & $Command
  $Code = $LASTEXITCODE
  if ($Code -ne 0) { throw "$Label failed with exit code $Code" }
  return $Output
}

function Invoke-CheckedNativeWithStdin(
  [string]$Label,
  [string]$FilePath,
  [string[]]$Arguments,
  [string]$InputText
) {
  $StartInfo = [System.Diagnostics.ProcessStartInfo]::new()
  $StartInfo.FileName = $FilePath
  $StartInfo.UseShellExecute = $false
  $StartInfo.RedirectStandardInput = $true
  $StartInfo.RedirectStandardOutput = $true
  $StartInfo.RedirectStandardError = $true
  foreach ($Argument in $Arguments) { $null = $StartInfo.ArgumentList.Add($Argument) }
  $Process = [System.Diagnostics.Process]::new()
  $Process.StartInfo = $StartInfo
  if (-not $Process.Start()) { throw "$Label failed to start" }
  $Bytes = [System.Text.UTF8Encoding]::new($false).GetBytes($InputText)
  $Process.StandardInput.BaseStream.Write($Bytes, 0, $Bytes.Length)
  $Process.StandardInput.BaseStream.Close()
  $Stdout = $Process.StandardOutput.ReadToEnd()
  $Stderr = $Process.StandardError.ReadToEnd()
  $Process.WaitForExit()
  if ($Process.ExitCode -ne 0) { throw "$Label failed with exit code $($Process.ExitCode): $Stderr" }
  if ($Stderr) { throw "$Label produced unexpected stderr: $Stderr" }
  return $Stdout
}

function Get-TextSha256([string]$Text) {
  $Bytes = [System.Text.UTF8Encoding]::new($false).GetBytes($Text)
  $Hash = [System.Security.Cryptography.SHA256]::HashData($Bytes)
  return [Convert]::ToHexString($Hash).ToLowerInvariant()
}

function Write-AcceptanceJournalAtomic([string]$Path, [System.Collections.IDictionary]$Journal) {
  $Directory = Split-Path -Parent $Path
  $null = [System.IO.Directory]::CreateDirectory($Directory)
  $TempPath = "$Path.$PID.tmp"
  $BackupPath = "$Path.$PID.bak"
  $Json = $Journal | ConvertTo-Json -Depth 100
  [System.IO.File]::WriteAllText($TempPath, $Json + "`n", [System.Text.UTF8Encoding]::new($false))
  try {
    if (Test-Path -LiteralPath $Path) {
      [System.IO.File]::Replace($TempPath, $Path, $BackupPath, $true)
      if (Test-Path -LiteralPath $BackupPath) { [System.IO.File]::Delete($BackupPath) }
    } else {
      [System.IO.File]::Move($TempPath, $Path)
    }
  } finally {
    if (Test-Path -LiteralPath $TempPath) { [System.IO.File]::Delete($TempPath) }
  }
  $RoundTrip = Get-Content -LiteralPath $Path -Raw | ConvertFrom-Json -Depth 100
  if ([string]$RoundTrip.state -ne [string]$Journal.state) { throw 'acceptance journal atomic round-trip failed' }
}

function Set-AcceptanceJournalState(
  [System.Collections.IDictionary]$Journal,
  [string]$Path,
  [string]$State,
  [System.Collections.IDictionary]$Fields
) {
  foreach ($Key in $Fields.Keys) { $Journal[$Key] = $Fields[$Key] }
  $Journal['state'] = $State
  $Journal['updated_at_utc'] = [DateTimeOffset]::UtcNow.ToString('O')
  Write-AcceptanceJournalAtomic $Path $Journal
}

function Assert-GitIndexClean([string]$Repo) {
  & git -C $Repo diff --cached --quiet
  $Code = $LASTEXITCODE
  if ($Code -eq 1) { throw "index is not clean: $Repo" }
  if ($Code -ne 0) { throw "cannot inspect index: $Repo (exit $Code)" }
}

function Get-GitRefOrNull([string]$Repo, [string]$FullRef) {
  $Output = & git -C $Repo rev-parse --verify --quiet $FullRef
  $Code = $LASTEXITCODE
  if ($Code -eq 0) { return (($Output -join "`n").Trim()) }
  if ($Code -eq 1) { return $null }
  throw "cannot inspect ref $FullRef in $Repo (exit $Code)"
}

function Ensure-GitRefsAtomically([string]$Repo, [System.Collections.IDictionary]$Desired) {
  $Creates = [System.Collections.Generic.List[string]]::new()
  foreach ($Name in $Desired.Keys) {
    $FullRef = "refs/heads/$Name"
    $Expected = [string]$Desired[$Name]
    $Existing = Get-GitRefOrNull $Repo $FullRef
    if ($null -eq $Existing) {
      $Creates.Add("create $FullRef $Expected")
    } elseif ($Existing -ne $Expected) {
      throw "$FullRef exists at $Existing, expected $Expected"
    }
  }
  if ($Creates.Count -gt 0) {
    $Transaction = @('start') + @($Creates) + @('prepare', 'commit')
    $null = Invoke-CheckedNativeWithStdin 'atomic ref transaction' 'git' `
      @('-C', $Repo, 'update-ref', '--stdin') (($Transaction -join "`n") + "`n")
  }
  foreach ($Name in $Desired.Keys) {
    $Actual = Get-GitRefOrNull $Repo "refs/heads/$Name"
    if ($Actual -ne [string]$Desired[$Name]) { throw "post-check failed for refs/heads/$Name" }
  }
}

function Stage-ExactManifestVersion([string]$Repo, [string]$OldVersion, [string]$NewVersion) {
  Assert-GitIndexClean $Repo
  $HeadLines = @((Invoke-CheckedNative 'read HEAD manifest' { git -C $Repo show HEAD:workspace.yaml }) | ForEach-Object { $_ -replace "`r$", '' })
  $IdMatches = @(0..($HeadLines.Count - 1) | Where-Object { $HeadLines[$_] -match '^\s*"id"\s*:\s*"ChatGPT-Fix"\s*,' })
  if ($IdMatches.Count -ne 1) { throw 'HEAD manifest must contain exactly one ChatGPT-Fix project' }
  $Start = $IdMatches[0]
  $NextIds = @((($Start + 1)..($HeadLines.Count - 1)) | Where-Object { $HeadLines[$_] -match '^\s*"id"\s*:' })
  $End = if ($NextIds.Count -gt 0) { $NextIds[0] - 1 } else { $HeadLines.Count - 1 }
  $VersionMatches = @($Start..$End | Where-Object { $HeadLines[$_] -match ('^\s*"current"\s*:\s*"' + [regex]::Escape($OldVersion) + '"\s*,?\s*$') })
  if ($VersionMatches.Count -ne 1) { throw "HEAD ChatGPT-Fix current must equal $OldVersion exactly once" }
  $LineIndex = $VersionMatches[0]
  $OldLine = $HeadLines[$LineIndex]
  $NewLine = $OldLine -replace ('"' + [regex]::Escape($OldVersion) + '"'), ('"' + $NewVersion + '"')

  $LineNo = $LineIndex + 1
  $VersionPatch = @(
    'diff --git a/workspace.yaml b/workspace.yaml',
    '--- a/workspace.yaml',
    '+++ b/workspace.yaml',
    "@@ -$LineNo,1 +$LineNo,1 @@",
    "-$OldLine",
    "+$NewLine"
  ) -join "`n"

  $Work = Get-Content -LiteralPath (Join-Path $Repo 'workspace.yaml') -Raw | ConvertFrom-Json -Depth 100
  $WorkProjects = @($Work.projects | Where-Object { $_.id -eq 'ChatGPT-Fix' })
  if ($WorkProjects.Count -ne 1) { throw 'working manifest must contain exactly one ChatGPT-Fix project' }
  $WorkVersion = [string]$WorkProjects[0].version.current
  if ($WorkVersion -eq $OldVersion) {
    $null = Invoke-CheckedNativeWithStdin 'dry-check exact manifest worktree hunk' 'git' `
      @('-C', $Repo, 'apply', '--check', '--unidiff-zero', '-') ($VersionPatch + "`n")
    $null = Invoke-CheckedNativeWithStdin 'apply exact manifest version hunk to worktree' 'git' `
      @('-C', $Repo, 'apply', '--unidiff-zero', '-') ($VersionPatch + "`n")
  } elseif ($WorkVersion -ne $NewVersion) {
    throw "working manifest ChatGPT-Fix current is $WorkVersion, expected $OldVersion or $NewVersion"
  }
  $WorkAfter = Get-Content -LiteralPath (Join-Path $Repo 'workspace.yaml') -Raw | ConvertFrom-Json -Depth 100
  $WorkAfterProjects = @($WorkAfter.projects | Where-Object { $_.id -eq 'ChatGPT-Fix' })
  if ($WorkAfterProjects.Count -ne 1 -or [string]$WorkAfterProjects[0].version.current -ne $NewVersion) {
    throw "working manifest ChatGPT-Fix current must equal $NewVersion after exact patch"
  }

  $null = Invoke-CheckedNativeWithStdin 'dry-check exact manifest staged hunk' 'git' `
    @('-C', $Repo, 'apply', '--cached', '--check', '--unidiff-zero', '-') ($VersionPatch + "`n")
  $null = Invoke-CheckedNativeWithStdin 'stage exact manifest version hunk' 'git' `
    @('-C', $Repo, 'apply', '--cached', '--unidiff-zero', '-') ($VersionPatch + "`n")

  $ExpectedLines = @($HeadLines)
  $ExpectedLines[$LineIndex] = $NewLine
  $IndexLines = @((Invoke-CheckedNative 'read staged manifest' { git -C $Repo show :workspace.yaml }) | ForEach-Object { $_ -replace "`r$", '' })
  if (($IndexLines -join "`n") -cne ($ExpectedLines -join "`n")) {
    throw 'staged manifest differs from HEAD by more than the exact ChatGPT-Fix version line'
  }
  $StagedNames = @(Invoke-CheckedNative 'inspect root staged paths' { git -C $Repo diff --cached --name-only })
  if ($StagedNames.Count -ne 1 -or $StagedNames[0] -ne 'workspace.yaml') {
    throw 'root staged set is not exactly workspace.yaml'
  }
  $null = Invoke-CheckedNative 'check staged manifest' { git -C $Repo diff --cached --check }
  return $VersionPatch
}

e8be93f29c7ec22f85c31b5ad7eff9f266f70a04 = ((Invoke-CheckedNative 'read child preimage OID' { git -C $ChildRoot rev-parse HEAD }) -join "`n").Trim()
$RootPreimageOid = ((Invoke-CheckedNative 'read root preimage OID' { git -C $CanonicalRoot rev-parse HEAD }) -join "`n").Trim()
$TaskShellRootOid = ((Invoke-CheckedNative 'read task-shell root OID' { git -C $TaskRoot rev-parse HEAD }) -join "`n").Trim()
if ($TaskShellRootOid -ne $ExpectedCurrentWorkRootOid) {
  throw "task-shell root OID $TaskShellRootOid does not match frozen task-start OID $ExpectedCurrentWorkRootOid"
}
$CurrentWorkCatalogName = "codex/ChatGPT-Fix/$CurrentWorkBranch"
$CurrentWorkRootOid = Get-GitRefOrNull $CanonicalRoot "refs/heads/$CurrentWorkCatalogName"
if ($null -eq $CurrentWorkRootOid) {
  if (-not $AllowCreateCurrentWorkRef) { throw "missing immutable current-work ref: $CurrentWorkCatalogName" }
  if ($ExpectedCurrentWorkRootOid -ne $RootPreimageOid) {
    throw 'a missing current-work ref may only be created at the verified root preimage OID'
  }
  $CurrentWorkRootOid = $ExpectedCurrentWorkRootOid
} elseif ($CurrentWorkRootOid -ne $ExpectedCurrentWorkRootOid) {
  throw "current-work ref $CurrentWorkCatalogName is $CurrentWorkRootOid, expected frozen task-start $ExpectedCurrentWorkRootOid"
}

if (Test-Path -LiteralPath $JournalPath) {
  $ExistingJournal = Get-Content -LiteralPath $JournalPath -Raw | ConvertFrom-Json -Depth 100
  throw "acceptance journal already exists at state $($ExistingJournal.state); execute the documented recovery row instead of starting a new transaction"
}
Assert-GitIndexClean $ChildRoot
Assert-GitIndexClean $TaskRoot
Assert-GitIndexClean $CanonicalRoot
$CanonicalChildIsSeparate = -not [System.StringComparer]::OrdinalIgnoreCase.Equals(
  [System.IO.Path]::GetFullPath($ChildRoot).TrimEnd('\'),
  [System.IO.Path]::GetFullPath($CanonicalChild).TrimEnd('\')
)
if ($CanonicalChildIsSeparate) {
  $CanonicalTracked = @(Invoke-CheckedNative 'inspect canonical child before acceptance' { git -C $CanonicalChild status --porcelain --untracked-files=no })
  if ($CanonicalTracked.Count -gt 0) { throw 'canonical child has tracked/index changes; stop before acceptance transaction' }
}
$AcceptanceJournal = [ordered]@{
  schema = 'chatgpt_fix.acceptance_journal.v1'
  phase = $PhaseId
  state = 'PREPARED'
  child_preimage_oid = e8be93f29c7ec22f85c31b5ad7eff9f266f70a04
  root_preimage_oid = $RootPreimageOid
  task_shell_root_oid = $TaskShellRootOid
  current_work_ref = $CurrentWorkCatalogName
  expected_current_work_root_oid = $ExpectedCurrentWorkRootOid
  old_version = $OldVersion
  new_version = $NewVersion
  phase_allowed_paths = @($PhaseAllowedPaths)
  created_at_utc = [DateTimeOffset]::UtcNow.ToString('O')
  updated_at_utc = [DateTimeOffset]::UtcNow.ToString('O')
}
Write-AcceptanceJournalAtomic $JournalPath $AcceptanceJournal

$null = Invoke-CheckedNative 'stage phase allowlist' { git -C $ChildRoot add -- $PhaseAllowedPaths }
$null = Invoke-CheckedNative 'check child candidate' { git -C $ChildRoot diff --cached --check }
$CandidatePaths = @(Invoke-CheckedNative 'inspect child candidate paths' { git -C $ChildRoot diff --cached --name-only })
if ($CandidatePaths.Count -eq 0 -or @($CandidatePaths | Where-Object { $_ -notin $PhaseAllowedPaths }).Count -gt 0) {
  throw 'candidate staged paths are empty or outside the phase allowlist'
}
$null = Invoke-CheckedNative 'commit child candidate' { git -C $ChildRoot commit -m $CandidateMessage }
b7c117f58d7b5875f781fe14ca501fd1b87b4a7b = ((Invoke-CheckedNative 'read candidate OID' { git -C $ChildRoot rev-parse HEAD }) -join "`n").Trim()
Set-AcceptanceJournalState $AcceptanceJournal $JournalPath 'CANDIDATE_COMMITTED' ([ordered]@{
  candidate_oid = b7c117f58d7b5875f781fe14ca501fd1b87b4a7b
})

$ManifestVersionPatch = Stage-ExactManifestVersion $CanonicalRoot $OldVersion $NewVersion
see acceptance journal manifest_patch_sha256 = Get-TextSha256 ($ManifestVersionPatch + "`n")
Set-AcceptanceJournalState $AcceptanceJournal $JournalPath 'ROOT_STAGED' ([ordered]@{
  manifest_patch_sha256 = see acceptance journal manifest_patch_sha256
})
$null = Invoke-CheckedNative 'show exact root staged diff' { git -C $CanonicalRoot diff --cached -- workspace.yaml }
$null = Invoke-CheckedNative 'commit root governance version' { git -C $CanonicalRoot commit -m "chore: accept ChatGPT Fix $NewVersion" }
45e83ae69e07a35f5f860316989e25adcafd8c00 = ((Invoke-CheckedNative 'read root governance OID' { git -C $CanonicalRoot rev-parse HEAD }) -join "`n").Trim()
Set-AcceptanceJournalState $AcceptanceJournal $JournalPath 'ROOT_GOVERNANCE_COMMITTED' ([ordered]@{
  root_governance_oid = 45e83ae69e07a35f5f860316989e25adcafd8c00
})

# 人工/代理把 45e83ae69e07a35f5f860316989e25adcafd8c00、$ManifestVersionPatch 摘要与最终证据写入执行书 receipt 后再继续。
try {
  $null = Invoke-CheckedNative 'stage final receipt' { git -C $ChildRoot add -- '计划书.md' '执行书.md' }
  $null = Invoke-CheckedNative 'check final receipt' { git -C $ChildRoot diff --cached --check }
  $ReceiptPaths = @(Invoke-CheckedNative 'inspect final receipt paths' { git -C $ChildRoot diff --cached --name-only })
  if ($ReceiptPaths.Count -eq 0 -or @($ReceiptPaths | Where-Object { $_ -notin @('计划书.md', '执行书.md') }).Count -gt 0) {
    throw 'final receipt staged paths are empty or outside the document allowlist'
  }
  $null = Invoke-CheckedNative 'commit child accepted receipt' { git -C $ChildRoot commit -m $ReceiptMessage }
} catch {
  & git -C $ChildRoot diff --cached --quiet
  $ChildIndexCode = $LASTEXITCODE
  if ($ChildIndexCode -eq 1) {
    $null = Invoke-CheckedNative 'unstage failed final receipt attempt' {
      git -C $ChildRoot restore --staged -- '计划书.md' '执行书.md'
    }
  } elseif ($ChildIndexCode -ne 0) {
    throw "cannot inspect child index during compensation (exit $ChildIndexCode)"
  }
  $null = Stage-ExactManifestVersion $CanonicalRoot $NewVersion $OldVersion
  $null = Invoke-CheckedNative 'commit bounded root rollback' { git -C $CanonicalRoot commit -m "chore: roll back ChatGPT Fix manifest to $OldVersion" }
  $RollbackRootOid = ((Invoke-CheckedNative 'read rollback root OID' { git -C $CanonicalRoot rev-parse HEAD }) -join "`n").Trim()
  Set-AcceptanceJournalState $AcceptanceJournal $JournalPath 'ROLLED_BACK' ([ordered]@{
    rollback_root_oid = $RollbackRootOid
    failure = [string]$_
  })
  throw
}
$AcceptedCommit = ((Invoke-CheckedNative 'read accepted child OID' { git -C $ChildRoot rev-parse HEAD }) -join "`n").Trim()
Set-AcceptanceJournalState $AcceptanceJournal $JournalPath 'ACCEPTED_COMMITTED' ([ordered]@{
  accepted_child_oid = $AcceptedCommit
})

Ensure-GitRefsAtomically $ChildRoot ([ordered]@{
  $ReleaseBranch = $AcceptedCommit
  $NextWorkBranch = $AcceptedCommit
})

Ensure-GitRefsAtomically $CanonicalRoot ([ordered]@{
  $CurrentWorkCatalogName = $CurrentWorkRootOid
  "codex/ChatGPT-Fix/$ReleaseBranch" = 45e83ae69e07a35f5f860316989e25adcafd8c00
  "codex/ChatGPT-Fix/$NextWorkBranch" = 45e83ae69e07a35f5f860316989e25adcafd8c00
})
Set-AcceptanceJournalState $AcceptanceJournal $JournalPath 'REFS_READY' ([ordered]@{
  current_work_root_oid = $CurrentWorkRootOid
  release_root_oid = 45e83ae69e07a35f5f860316989e25adcafd8c00
  next_work_root_oid = 45e83ae69e07a35f5f860316989e25adcafd8c00
})

if ($CanonicalChildIsSeparate) {
  $CanonicalTracked = @(Invoke-CheckedNative 'inspect canonical child before release alignment' { git -C $CanonicalChild status --porcelain --untracked-files=no })
  if ($CanonicalTracked.Count -gt 0) { throw 'canonical child has tracked/index changes; stop before release alignment' }
}
$null = Invoke-CheckedNative 'align canonical child to release' { git -C $CanonicalChild switch $ReleaseBranch }

$ChildBranches = @(Invoke-CheckedNative 'enumerate child local branches' {
  git -C $ChildRoot for-each-ref refs/heads --format='%(refname:short)'
})
if ($ChildBranches.Count -eq 0) { throw 'child branch catalog is empty' }
foreach ($ChildBranch in $ChildBranches) {
  $CatalogRef = "refs/heads/codex/ChatGPT-Fix/$ChildBranch"
  if ($null -eq (Get-GitRefOrNull $CanonicalRoot $CatalogRef)) {
    throw "sync preview found missing catalog ref: $CatalogRef"
  }
}
$SyncOutput = @(Invoke-CheckedNative 'sync ChatGPT-Fix branches' {
  python (Join-Path $CanonicalRoot 'repo-control\repo_control.py') sync-branches --project ChatGPT-Fix
})
$SyncResult = (($SyncOutput -join "`n") | ConvertFrom-Json -Depth 100)
$SyncBranches = @($SyncResult.projects.'ChatGPT-Fix'.branches)
$ReportedBranches = @($SyncBranches | ForEach-Object { [string]$_.child_branch } | Sort-Object)
$ExpectedBranches = @($ChildBranches | Sort-Object)
if (($ReportedBranches -join "`n") -cne ($ExpectedBranches -join "`n")) {
  throw 'sync branch set differs from preview'
}
if (@($SyncBranches | Where-Object { $_.status -ne 'preserved_existing' }).Count -gt 0) {
  throw 'sync unexpectedly created or changed a catalog ref'
}
$BranchSyncEvents = @($SyncBranches | ForEach-Object { [string]$_.event })
$ExpectedEventRoot = [System.IO.Path]::GetFullPath((Join-Path $CanonicalRoot '.repo-control\events\ChatGPT-Fix\branch-sync'))
$ExpectedEventPrefix = $ExpectedEventRoot.TrimEnd('\') + '\'
foreach ($EventPath in $BranchSyncEvents) {
  $FullEventPath = [System.IO.Path]::GetFullPath($EventPath)
  if (-not $FullEventPath.StartsWith($ExpectedEventPrefix, [System.StringComparison]::OrdinalIgnoreCase)) {
    throw "branch-sync event escaped expected directory: $FullEventPath"
  }
  if (-not (Test-Path -LiteralPath $FullEventPath -PathType Leaf)) { throw "branch-sync event is missing: $FullEventPath" }
  $Event = Get-Content -LiteralPath $FullEventPath -Raw | ConvertFrom-Json -Depth 100
  if ($Event.project -ne 'ChatGPT-Fix' -or $Event.task -ne 'branch-sync' -or $Event.action -ne 'sync_branch_ref') {
    throw "unexpected branch-sync event content: $FullEventPath"
  }
}
Set-AcceptanceJournalState $AcceptanceJournal $JournalPath 'SYNCED' ([ordered]@{
  branch_sync_events = @($BranchSyncEvents)
})
$null = Invoke-CheckedNative 'run workspace doctor' {
  python (Join-Path $CanonicalRoot 'repo-control\repo_control.py') doctor
}
Set-AcceptanceJournalState $AcceptanceJournal $JournalPath 'VERIFIED' ([ordered]@{
  doctor = 'PASS'
})
```

版本事务禁止 `git add -A` 和 `git add workspace.yaml`。`Stage-ExactManifestVersion` 同时校验 HEAD 旧值、working tree 旧/新值、目标项目唯一性、单行单 hunk、worktree/index 两次 `git apply --check`、staged blob 与预期逐行相等以及 staged path 唯一性；working tree 尚为旧值时也只应用同一精确 hunk。child/root branch refs 通过一个 `update-ref --stdin` transaction 原子创建；已存在同 OID 可重跑，异 OID 停止。root governance commit 后若 child accepted receipt 无法形成，必须先用同一函数形成新的 bounded rollback commit 再退出；其他后续失败保留已验证 OID并允许幂等重跑，不移动任何既有 ref。

接受事务还必须在 `%TEMP%\ChatGPT-Fix-P<N>-acceptance-journal.json` 保存不含 thread/task UUID 的原子 journal；每次状态前后都记录 child/root preimage OID、目标版本、manifest patch SHA-256、已验证输出和下一恢复动作。恢复只能按下表继续，不得猜测：

| journal state | 已允许存在的状态 | 恢复动作 |
|---|---|---|
| `PREPARED` | 无新 commit/ref | 重新验证 preimage 后从 candidate 开始 |
| `CANDIDATE_COMMITTED` | child candidate OID；root 未提交 | 验证 candidate 后执行精确 root hunk |
| `ROOT_STAGED` | root index 仅含精确 version hunk；root branch 未提交 | 校验 patch hash/staged blob 后提交 root governance |
| `ROOT_GOVERNANCE_COMMITTED` | candidate + root OID；accepted receipt 未形成 | 完成 receipt；若放弃则执行 bounded root rollback |
| `ACCEPTED_COMMITTED` | child accepted + root OID；release/catalog refs 未创建 | 原子创建/校验 refs |
| `REFS_READY` | refs OID 全部匹配；canonical/sync 未完成 | clean-check 后对齐 canonical，预览并运行 sync |
| `SYNCED` | branch-sync events 已记录 | 运行 doctor、生成冻结 handoff并进入 ACK |
| `VERIFIED` | doctor PASS；handoff 尚未 ACK | 生成/校验冻结 handoff并创建或复用 successor task |
| `ROLLED_BACK` | root manifest 已用 bounded commit恢复旧版本；无 release handoff | 保留证据并人工决定新一轮 journal，不复用旧事务 |

journal 更新必须使用临时文件加 atomic replace；任何 journal 与实际 Git 状态不一致都停止并交人工审查。最终 handoff 记录 journal 路径/hash，P1 ACK 后可保留但不自动删除。

阶段 handoff 前必须记录：child work/release branch 与 OID、root technical ref 与 OID、VERSION、manifest version、测试结果、授权消费和仓库外写入。

## 5. 审查与提交协议

每个实现任务遵循：实现代理完成并自审 -> 规格审查代理确认无缺项/越界 -> 质量审查代理确认可维护性/安全性 -> 主代理运行验证并提交。

P0 以及每个后续阶段的最低文档/仓库检查：

```powershell
rg -n "T[B]D|T[O]DO|待[定]|以后再[说]" (Join-Path $ChildRoot '计划书.md') (Join-Path $ChildRoot '执行书.md')
git -C $ChildRoot diff --check
git -C $ChildRoot diff --stat
git -C $ChildRoot status --short --branch
```

第一条预期无匹配；其余命令预期无 whitespace error，且只包含本阶段允许写集。提交前必须把 diff/验证证据交给审查代理；禁止用实现代理自审替代两级审查。

## 6. P0 执行卡：两书与治理基线

**状态：** ACTIVE
**Goal：** 固定两书、阶段/Goal/版本/EXE/handoff 协议，不实现产品代码。
**工作分支：** `work/p0-plan-governance`
**目标分支：** `release/0.2.0`
**tracked-content 写集：** `计划书.md`、`执行书.md`、`.gitignore` 中仅 `.squad/` 与 `locks/*.receive.lock` 两条规则、`VERSION`、根 manifest 的 ChatGPT-Fix 版本字段。
**治理元数据写集：** child/root commits 与 current-work/release/next-work refs；`.repo-control/events/ChatGPT-Fix/branch-sync/*.json`；successor task shell 的 `.repo-control/state/ChatGPT-Fix/<task>.json`、`.repo-control/events/ChatGPT-Fix/<task>/*.json`、managed child worktree/`attached\ChatGPT-Fix` mount；Codex Goal/host task metadata和 `%TEMP%` acceptance journal/handoff。tracked receipt 记录非私密父路径/不可逆 task alias，UUID-bearing 实际路径只进入临时 handoff；禁止共享 `tasks.json`。
**禁止：** 运行 `scripts/chatgpt-fix-doctor.ps1`、安装器、Packer/Launcher 产品代码、进程或 `.codex` 操作。

P0 调用第 4 节接受事务时必须使用以下精确变量：

```powershell
$PhaseId = 'P0'
$JournalPath = Join-Path $env:TEMP 'ChatGPT-Fix-P0-acceptance-journal.json'
$PhaseAllowedPaths = @('.gitignore', '计划书.md', '执行书.md', 'VERSION')
$OldVersion = '0.1.0'
$NewVersion = '0.2.0'
$CandidateMessage = 'docs: establish staged execution books'
$ReceiptMessage = 'docs: record P0 acceptance receipt'
$CurrentWorkBranch = 'work/p0-plan-governance'
$ExpectedCurrentWorkRootOid = '2c5982acb92b45543475fcd43f33c3f73036c7ef'
$AllowCreateCurrentWorkRef = $true
$ReleaseBranch = 'release/0.2.0'
$NextWorkBranch = 'work/p1-offline-core'
```

- [x] 读取 `workspace.yaml`、child `AGENTS.md`、历史方向报告与 doctor 源码。
- [x] 核对 child clean baseline、root index、`repo-control doctor`。
- [x] 建立 `work/p0-plan-governance`。
- [x] 创建 P0 Goal 和人类可读任务标题。
- [x] 核对本机构建链：Rust 可用；.NET SDK/外部 installer packer/signtool 不可用。
- [x] 使用 squad/QoderCLI 完成只读旁路评估；撤回 `squad init` 自动注入的代理说明，只忽略本地 squad 状态。
- [x] 新增并自审两书。
- [x] 完成规格符合性审查并修正缺项；门下第三轮 PASS。
- [x] 完成执行质量审查并修正风险；尚书 rev-9 最终 PASS。
- [x] 运行 placeholder scan、`git diff --check`、secret/private-ID、PowerShell 解析和阶段一致性检查。
- [ ] 更新 `VERSION=0.2.0` 与 root manifest，确认 `repo-control doctor` 通过。
- [ ] 提交 accepted commit，并创建 `release/0.2.0`。
- [ ] 预建 `work/p1-offline-core`，运行 `sync-branches`。
- [ ] 在 `%TEMP%` 创建 P0->P1 handoff。
- [ ] 创建项目任务 `ChatGPT修复｜P1离线核心与EXE`，验证 project/worktree/attached child。
- [ ] 更新 P0 receipt，标记 P0 Goal complete，当前对话停止修改。

P0 建议 commit：`docs: establish staged execution books`。版本接受 commit 若单独提交：`chore: accept ChatGPT Fix 0.2.0 governance baseline`。

## 7. P1 执行卡：离线核心、fixture 与首批 EXE

**状态：** ACTIVE，A1 已授予，P0 handoff 与 START_GOAL 已接受；等待最终双审与接受事务。
**Goal：** 在不读取 live package/process/.codex 的条件下建立 Rust workspace、schema、fixtures 和三个可执行程序的离线 dry-run。
**分支：** `work/p1-offline-core` -> `release/0.3.0`。

P1 接受事务必须设置 `$CurrentWorkBranch='work/p1-offline-core'`、`$ExpectedCurrentWorkRootOid=<P0 冻结 handoff 中的 next-work root OID>` 与 `$AllowCreateCurrentWorkRef=$false`，并验证 task-shell HEAD/current-work ref 都等于该 task-start OID；不得把 current-work ref 推进到 P1 governance OID。

允许创建：

```text
Cargo.toml
Cargo.lock
rust-toolchain.toml
src/chatgpt-fix-core/
src/chatgpt-fix-packer/
src/chatgpt-fix-manager/
src/chatgpt-fix-launcher/
tests/fixtures/
records/builds/
records/native-token-cost-integration-20260801.md（A10-NTC delegated evidence only）
checksums/
out/
.gitignore
```

要求：

- Core 定义 `chatgpt_fix.plan.v1`、`chatgpt_fix.baseline.v2`、`chatgpt_fix.launch.v1`、`chatgpt_fix.receipt.v1` 数据契约。
- Packer/Manager/Launcher 均支持 `--version`；Packer 和 Manager 只接受 fixture root，live path 必须以明确错误退出。
- Fixture 覆盖多版本、错误架构、未知 publisher、损坏 manifest、reparse path、磁盘预算不足、hash mismatch。
- P1 Launcher 不启动任何程序；调用 live launch 命令必须返回拒绝状态。
- 构建输出保存在未跟踪的 `out/0.3.0/win-x64/`；跟踪 build receipt、SHA-256 和依赖许可清单。
- P1 workspace 使用 Rust 标准库，不引入第三方 crates；不得访问 crates.io、下载 toolchain/target/component 或写用户级 Cargo/rustup cache。若离线预检缺失，停止并请求 A1n，不得让 `rust-toolchain.toml` 触发自动安装。
- `CARGO_HOME` 与 `CARGO_TARGET_DIR` 都固定到 ignored `out/0.3.0/` 内；`CARGO_NET_OFFLINE=true`，所有 cargo resolve/build 命令显式 `--offline --locked`（不支持该参数的 `cargo fmt` 仍继承 offline 环境）。
- 首次创建 manifests 后使用 checked `cargo generate-lockfile --offline` 生成并提交 `Cargo.lock`；后续命令一律 `--locked`，lockfile 变化即验收失败。

验收命令：

```powershell
$env:RUSTUP_TOOLCHAIN = 'stable-x86_64-pc-windows-msvc'
$env:CARGO_NET_OFFLINE = 'true'
$env:CARGO_HOME = Join-Path $ChildRoot 'out\0.3.0\cargo-home'
$env:CARGO_TARGET_DIR = Join-Path $ChildRoot 'out\0.3.0\cargo-target'

$Rustc = ((Invoke-CheckedNative 'rustc preflight' { rustc -V }) -join "`n").Trim()
if ($Rustc -notmatch '^rustc 1\.97\.1 ') { throw "unexpected rustc: $Rustc" }
$InstalledTargets = @(Invoke-CheckedNative 'target preflight' { rustup target list --installed })
if ('x86_64-pc-windows-msvc' -notin $InstalledTargets) { throw 'MSVC target is not preinstalled; A1n required' }
$InstalledComponents = @(Invoke-CheckedNative 'component preflight' { rustup component list --installed })
if (@($InstalledComponents | Where-Object { $_ -like 'rustfmt-*' }).Count -eq 0 -or
    @($InstalledComponents | Where-Object { $_ -like 'clippy-*' }).Count -eq 0) {
  throw 'rustfmt/clippy are not preinstalled; A1n required'
}

$null = Invoke-CheckedNative 'cargo metadata offline' { cargo metadata --offline --locked --no-deps --format-version 1 }
$null = Invoke-CheckedNative 'cargo fmt' { cargo fmt --all -- --check }
$null = Invoke-CheckedNative 'cargo clippy offline' { cargo clippy --offline --locked --workspace --all-targets -- -D warnings }
$null = Invoke-CheckedNative 'cargo test offline' { cargo test --offline --locked --workspace }
$null = Invoke-CheckedNative 'cargo build offline' {
  cargo build --offline --locked --workspace --release --target x86_64-pc-windows-msvc
}
$null = Invoke-CheckedNative 'P1 diff check' { git -C $ChildRoot diff --check }
```

禁止：live `Get-AppxPackage`、doctor、shortcut/process/config/.codex/WindowsApps 操作。

## 8. P2-P8 阶段入口

本节各项仅为 `DRAFT` 入口摘要。任何 P2-P8 阶段不得仅凭下列段落进入 `AUTHORIZED` 或创建 Goal。阶段 P<N> 必须在自己的单一 Goal 收尾、candidate commit 之前，以 A0 治理写集追加并审查 P<N+1> 的详细执行卡（允许写集、禁止动作、精确命令、测试、失败补偿、授权消费），取得门下规格 PASS、尚书质量 PASS和下一阶段所需用户授权；不得另建规划 Goal。若卡或授权未通过，P<N> Goal 保持 active且不创建 successor task。新任务只读取已获批卡，收到 `START_GOAL` 后才建立 P<N+1> 的唯一 Goal。

### P2 / `release/0.4.0`

**状态：** `VERIFYING / AWAITING_ACCEPTANCE_TRANSACTION`。A2-P2 已逐字确认授予；P2 离线门禁（metadata/fmt/clippy/test 141/build）与 live acceptance（inspect/plan/review 字节一致）全部 PASS；A2-P2 receipt 已记录；doctor v2 proposal 与 P3 详细卡已追加并通过检查；A3 已由用户明确授予。下一步：运行第 4 节接受事务创建 `release/0.4.0`，随后进入 RELEASE-BRANCHED / HANDED-OFF。

**Goal：** 在不读取 process、`.codex` 或 package `LocalState`，且不改变任何 live 状态的条件下，让 Packer 成为 `OpenAI.Codex` 当前用户包的唯一 canonical `inspect`/`plan` 生成者；Manager 只解析、校验并原样展示 Packer 计划；形成 doctor v2 源码提案但不运行 doctor。

**分支与版本：** `work/p2-live-inspection` -> `release/0.4.0`；workspace/package/build 输出版本固定 `0.4.0`。P2 task 从 P1 accepted commit 预建分支 materialize，不得复用 P1 checkout。

**P2 唯一可消费授权：** `A0 + A1 + A2-P2`。A2-P2 只允许 Packer 启动固定的 `pwsh.exe -NoProfile -NonInteractive` 内嵌 probe，并由 probe 执行下列读取：

```text
Get-AppxPackage -Name 'OpenAI.Codex' -ErrorAction Stop
Get-AppxPackage -Name $DependencyName -ErrorAction Stop
读取所选 package InstallLocation 下的 AppxManifest.xml
读取 manifest Application.Executable 解析出的单个 primary EXE 的 metadata/bytes/SHA-256
读取 %APPDATA%\Microsoft\Windows\Start Menu\Programs\ChatGPT.lnk 的 TargetPath、Arguments、WorkingDirectory、IconLocation
```

`$DependencyName` 必须逐个来自已选 `OpenAI.Codex` manifest 的有限 `PackageDependency.Name` 集合，不允许空值、通配符或回退到全包枚举。probe 必须固定当前用户、package name `OpenAI.Codex`、publisher id `2p2nqsd0c76g0`、architecture `x64` 与上述 shortcut；CLI 不接受覆盖这些 live selector 的参数。

**tracked-content 写集：**

```text
Cargo.toml
Cargo.lock（必须保持仅四个 workspace package；允许 workspace version 的 0.3.0 -> 0.4.0 机械变化，出现新增 package/dependency 才停止并请求 A1n）
src/chatgpt-fix-core/
src/chatgpt-fix-packer/
src/chatgpt-fix-manager/
scripts/chatgpt-fix-p2-probe.ps1
tests/live-fixtures/
docs/chatgpt-fix-doctor-v2-proposal.md
records/builds/0.4.0-win-x64/
checksums/0.4.0-win-x64.sha256
计划书.md
执行书.md
VERSION（仅接受事务）
```

`src/chatgpt-fix-launcher/`、既有 `scripts/chatgpt-fix-doctor.ps1` 与 P1 evidence 不在 P2 产品写集；仅允许由 workspace version 的继承机制使 Launcher `--version` 变为 `0.4.0`，不得新增 Launcher live 行为。

**A0q 附件 allowlist：** 仅 `计划书.md`、`执行书.md`，且必须先通过 secret/private-ID scan；不得附加 probe script、synthetic/live JSON、build receipt、绝对路径或任何 runtime evidence。A0q advisory 不替代门下/刑部结论。

**task-local/ignored 写集：** `out/0.4.0/**`、`.repo-control` 本 task state/event、`%TEMP%\ChatGPT-Fix-P2-acceptance-journal.json` 与冻结 handoff。live probe stdout/stderr 只能保存在内存或 ignored `out/0.4.0/live-evidence/`；不得把 username、绝对 InstallLocation、shortcut 绝对路径或原始 live JSON 提交到 Git。

**永久禁止：**

- 不运行 `scripts/chatgpt-fix-doctor.ps1`；不修改它，只新增 doctor v2 proposal。
- 不执行无 `-Name` 的 `Get-AppxPackage`，不使用 `-AllUsers`，不读取其他 package。
- 不执行 `Get-Process`、`Get-CimInstance`、`Get-WmiObject`、perf counter 或任何 process tree/command line/handle 查询。
- 不读取或遍历 `.codex`、`CODEX_HOME`、`%LOCALAPPDATA%\Packages\OpenAI.Codex*\LocalState`、auth、token、history、session、SQLite 或 MCP 配置。
- 不执行 `Add/Remove-AppxPackage`，不写 `WindowsApps`，不创建/修改 shortcut，不修改配置，不启动 ChatGPT，不启动 package primary EXE，不终止/结束/清理任何进程。
- 不下载 crate/toolchain/component，不访问网络，不写用户 Cargo/rustup cache，不运行安装器，不提权，不签名，不发布。

**Core 契约：**

- 新增严格 UTF-8 JSON parser；拒绝 duplicate key、trailing data、非规范数字、无效 escape、控制字符、深度/长度超限。不得引入 serde 或第三方 crate。
- 新增 `chatgpt_fix.live_inspection.v1` typed contract，至少包含：probe source、package identity/full name/version/architecture/publisher/publisher id、install location、manifest SHA-256、primary executable relative path/bytes/SHA-256、manifest dependencies、shortcut 四项属性、probe 前后 identity/hash 一致性，以及 `codex_home_inspected=false`、`local_state_inspected=false`、`processes_inspected=false`。
- P2 所称 effective config 只包含 manifest `Application.Executable`/launch entry 与固定 shortcut 的 TargetPath/Arguments/WorkingDirectory/IconLocation；`.codex`、`CODEX_HOME`、package `LocalState` 与 MCP/history/auth 配置统一延后到 P7，P2 不得以“有效配置”为由读取。
- 新增 `PlanV1::from_json` 严格解析；parse -> validate -> canonical serialize 必须字节稳定。P2 live plan 继续输出 `chatgpt_fix.plan.v1`，其 `fixture_id` 固定为 `live-readonly`，不得把绝对 package/shortcut 路径写入 `BaselineV2.source` 或 action target。
- identity 只有同时满足 package name `OpenAI.Codex`、publisher id `2p2nqsd0c76g0`、`x64`、四段数值版本、PackageFullName 一致、manifest Identity 一致时才有效。manifest dependency 必须找到满足 Name/Publisher/MinVersion 的当前用户注册对象。
- InstallLocation、manifest、primary EXE 与 shortcut 的每个路径组件遇到 symlink/reparse、越界、缺失、大小/hash 漂移或 probe 前后不一致都 fail closed。多个最高版本、相同版本不同 identity 或并发 package 更新返回稳定拒绝码，不猜测选择。

**CLI 冻结：**

```text
ChatGPT-Fix-Packer inspect --probe-json <path>       # offline synthetic input，不消费 A2-P2
ChatGPT-Fix-Packer plan --probe-json <path>          # offline canonical plan，不消费 A2-P2
ChatGPT-Fix-Packer inspect --live-readonly           # 仅 A2-P2
ChatGPT-Fix-Packer plan --live-readonly              # 仅 A2-P2
ChatGPT-Fix-Manager review --plan-stdin              # stdin 严格 parse/validate/canonicalize
```

Packer live 路径只能启动字面程序名 `pwsh.exe`，参数固定 `-NoProfile -NonInteractive -Command -`，并把 tracked、编译期内嵌的 probe script 加上字面尾调用 `Invoke-ChatGptFixP2Probe -Mode Live` 送入 stdin；不得接受 `--shell`、`--script`、`--package`、`--shortcut` 或任意命令参数。fixture tests 只可把 `$SyntheticRoot` 绑定到任务 3 列出的六个 `tests/live-fixtures/` 目录之一，再直接调用 tracked script 的 `-Mode Fixture -FixtureRoot $SyntheticRoot`。probe 以单个 canonical JSON 文档输出到 stdout，stderr 非空、退出非零、额外 stdout 或 schema 不匹配均失败。Manager 不链接 probe 模块、不启动 Packer/PowerShell、不读取 package/shortcut，只消费 stdin plan；非 canonical 输入明确拒绝。

**实现任务与提交：** 每项使用 `superpowers:subagent-driven-development` 串行执行，先 TDD RED，再 GREEN、自审、门下规格审查、刑部质量审查；审查通过前不进入下一项。

1. Workspace 0.4.0 bootstrap：先锁定四个 package 的 `--version` RED；只改根 `Cargo.toml` workspace version，运行 `cargo generate-lockfile --offline`，证明 Cargo.lock 只有四个 workspace package/version 机械变化。提交 `build: start 0.4.0 live inspection`。
2. Core strict JSON 与 `live_inspection.v1`：用 duplicate key、invalid escape、trailing data、深度/长度、错误 identity/dependency/reparse/drift fixtures 形成 RED；实现 parse/validate/canonical serialize。提交 `feat(core): define live inspection contract`。
3. Probe script 与 synthetic snapshots：新增 `valid-current`、`publisher-mismatch`、`dependency-missing`、`shortcut-drift`、`concurrent-update`、`reparse-path`；PowerShell AST 测试拒绝 mutation/process/network cmdlet，并证明 fixture mode 不调用 `Get-AppxPackage`。提交 `feat(probe): add bounded package inspection`。
4. Packer canonical live inspect/plan：offline `--probe-json` 先覆盖全部 snapshot；实现固定 pwsh stdin adapter、stdout/stderr/exit guards 与唯一 plan 生成。提交 `feat(packer): add live readonly planning`。
5. Manager plan-view：先用 Packer canonical stdout、duplicate-key、noncanonical、rejected plan 写 stdin tests；实现 parse/validate/字节一致展示，不实现 discovery。提交 `feat(manager): validate canonical plans from stdin`。
6. Doctor v2 proposal：只描述未来把 package/shortcut/config/process 分成独立 probe 的接口和授权门；明确 P2 不运行/修改旧 doctor、不读取 process/`.codex`。提交 `docs: propose doctor v2 probe boundaries`。
7. 0.4.0 离线构建证据：延续 P1 typed receipts/checksum/license/SPDX；release EXE 只进入 ignored `out/0.4.0/win-x64/`。提交 `build: record 0.4.0 offline artifacts`。
8. 获得 A2-P2 后才执行一次 live acceptance；如 package 在 probe 前后变化，只允许在同一 P2 task 重跑相同只读命令，不扩展 selector。

**离线门禁（A2-P2 之前）：**

```powershell
$env:RUSTUP_TOOLCHAIN = 'stable-x86_64-pc-windows-msvc'
$env:RUSTUP_NO_UPDATE_CHECK = '1'
$env:CARGO_NET_OFFLINE = 'true'
$env:CARGO_HOME = Join-Path $ChildRoot 'out\0.4.0\cargo-home'
$env:CARGO_TARGET_DIR = Join-Path $ChildRoot 'out\0.4.0\cargo-target'

cargo metadata --offline --locked --no-deps --format-version 1
cargo fmt --all -- --check
cargo clippy --offline --locked --workspace --all-targets -- -D warnings
cargo test --offline --locked --workspace
cargo build --offline --locked --workspace --release --target x86_64-pc-windows-msvc
git -C $ChildRoot diff --check
```

PowerShell AST 门禁必须解析成功且 command inventory 不含：`Add-AppxPackage`、`Remove-AppxPackage`、`Set-Content`、`Add-Content`、`Out-File`、`Export-*`、`New-Item`、`Remove-Item`、`Move-Item`、`Copy-Item`、`Rename-Item`、`Start-Process`、`Stop-Process`、`Invoke-WebRequest`、`Invoke-RestMethod`、`Get-Process`、`Get-CimInstance`、`Get-WmiObject`。Rust product scan 必须证明 Launcher/Manager 没有 process start API，Packer 的唯一 `Command` target 是常量 `pwsh.exe` 且只可从 `--live-readonly` 分支到达。

**live acceptance（只在精确 A2-P2 授权与全部离线门禁通过后）：**

```powershell
$Packer = Join-Path $ChildRoot 'out\0.4.0\win-x64\ChatGPT-Fix-Packer.exe'
$Manager = Join-Path $ChildRoot 'out\0.4.0\win-x64\ChatGPT-Fix-Manager.exe'

$InspectJson = (& $Packer inspect --live-readonly) -join "`n"
if ($LASTEXITCODE -ne 0) { throw 'live inspect failed' }
$Inspection = $InspectJson | ConvertFrom-Json -Depth 100
if ($Inspection.schema -ne 'chatgpt_fix.live_inspection.v1' -or
    $Inspection.package.name -ne 'OpenAI.Codex' -or
    $Inspection.package.publisher_id -ne '2p2nqsd0c76g0' -or
    $Inspection.processes_inspected -ne $false -or
    $Inspection.codex_home_inspected -ne $false -or
    $Inspection.local_state_inspected -ne $false) { throw 'bounded inspection contract failed' }

$PlanJson = (& $Packer plan --live-readonly) -join "`n"
if ($LASTEXITCODE -ne 0) { throw 'live plan failed' }
$Reviewed = $PlanJson | & $Manager review --plan-stdin
if ($LASTEXITCODE -ne 0 -or (($Reviewed -join "`n") -cne $PlanJson)) {
    throw 'manager did not preserve canonical plan bytes'
}
```

live acceptance 同时要求：probe 内部 package/manifest/exe/shortcut before/after 快照一致；stdout 无绝对路径泄露到 tracked files；Packer/Manager 退出后不运行 doctor、不调用 Launcher、不产生 live state 写入。若 package 更新、shortcut 改变、依赖不满足或 reparse/identity 不可证明，阶段验收失败关闭；不得“选择最像的候选”继续。

**失败补偿：** live 路径只有读取，没有系统回滚动作。失败时保留 ignored `out/0.4.0/live-evidence/` 诊断与 receipt，标记 quarantine；不得自动删除、重试其他 package、终止 probe/ChatGPT 进程或修改 shortcut。tracked 代码问题走新 commit 修复并重过门下/刑部；授权边界问题回到用户，不把 A2-P2 升格为 A2。

**P2 完成条件：** 全部离线与 live acceptance PASS；A2-P2 receipt 明确记录实际命令、读取类别和全部 exclusion；P3 详细卡已在同一 P2 Goal 内追加并通过门下/刑部且 A3 已明确授权；然后才运行版本接受事务、创建 `release/0.4.0`/`work/p3-baseline-packer`、P2->P3 handoff。未取得 A3 时 P2 Goal 保持 active。

### Native token-cost integration 追加项 / `NTC-NATIVE-20260801`

**状态：** `DELEGATED_EVIDENCE_RECORDED`；用户已明确授权将当前 native token-cost 集成接回 ChatGPT-Fix，但本条不是 P1 离线验收，也不替代 A2-P2 精确授权。

- 当前 delegated evidence 记录在 `records/native-token-cost-integration-20260801.md`，只保存 normalized paths、版本、marker、hash、helper health 和 UI proof 状态；不保存用户名、thread/task UUID、raw `.codex` session 或 renderer dump。
- P1 只登记 receipt，不重复 app.asar 写入、重打包、Scheduled Task 变更、ChatGPT 重启或 live UI 读取；现有 P1 `process/config/state` 仍为 none。
- P2 只做 offline contract/proposal 和 launcher-owned overlay 识别输入，不把 token-cost 事项扩展为 A2-P2 的官方包 selector；P3/P4 负责 baseline overlay 的 manifest、hash、backup、generation、rollback 和 update reapply；P8 负责 installer/repair/doctor/helper health/UI canary 产品化。
- 当前磁盘注入 marker 与 archive/node check 不能证明 renderer UI 已显示；UI canary 保持 `PENDING`，后续只能使用安全、可逆、保留用户会话的验证方式。

**追加项接受条件：** 未来进入产品化阶段前，必须有 source/resource manifest、app.asar before/after SHA-256、可回滚 backup、helper task health receipt、reapply-on-update 测试、doctor 检查和独立授权；不得把一次性手工 patch 当作稳定安装状态。

### P3 / `release/0.5.0`

**状态：** `VERIFYING / AWAITING_ACCEPTANCE_TRANSACTION`。A3 已由用户明确授予；P3 离线门禁（metadata/fmt/clippy/test 148/build）全 PASS；PowerShell AST 门禁 PASS；stage/verify 端到端验证 PASS（stage→verified、白名单过滤、quarantine fail-closed、live WindowsApps 路径拒绝）；P3 build evidence（0.5.0 typed receipts）已提交；P4 详细卡已追加。下一步：运行第 4 节接受事务创建 `release/0.5.0`，随后进入 RELEASE-BRANCHED / HANDED-OFF。

**Goal：** 在 A3 明确授权后，Packer 增加 `stage`/`verify` 子命令，只写 `%LOCALAPPDATA%\Programs\ChatGPT-Fix\staging` 和最终 immutable baseline；不创建/修改 current pointer，不改 shortcut，不启动 ChatGPT。失败 staging 进入 quarantine，不自动删除。

**分支与版本：** `work/p3-baseline-packer` -> `release/0.5.0`；workspace/package/build 输出版本固定 `0.5.0`。P3 task 从 P2 accepted commit 预建分支 materialize，不得复用 P2 checkout。

**P3 唯一可消费授权：** `A0 + A1 + A3`。A3 只授权 Packer 在 `%LOCALAPPDATA%\Programs\ChatGPT-Fix\staging\` 与最终 immutable baseline 目录内创建/写入/重命名文件；不授权写 `current.json`/pointer、shortcut、WindowsApps、配置、`.codex`，不授权启动进程、终止进程或签名。

**tracked-content 写集：**

```text
Cargo.toml
Cargo.lock（仅 workspace version 0.4.0 -> 0.5.0 机械变化；新增 package/dependency 停止并请求 A1n）
src/chatgpt-fix-core/
src/chatgpt-fix-packer/
src/chatgpt-fix-manager/
scripts/chatgpt-fix-p2-probe.ps1（只允许保持 P2 冻结内容，如无需要不得修改）
tests/staging-fixtures/
docs/chatgpt-fix-doctor-v2-proposal.md（仅允许 P3 修正，不扩展 live 读取）
records/builds/0.5.0-win-x64/
checksums/0.5.0-win-x64.sha256
计划书.md
执行书.md
VERSION（仅接受事务）
```

`src/chatgpt-fix-launcher/`、`scripts/chatgpt-fix-doctor.ps1`、P2 evidence 不在 P3 产品写集；仅允许 workspace version 继承使 Launcher `--version` 变为 `0.5.0`，不得新增 Launcher 行为。

**task-local/ignored 写集：** `out/0.5.0/**`、`.repo-control` 本 task state/event、`%TEMP%\ChatGPT-Fix-P3-acceptance-journal.json` 与冻结 handoff。staging 测试只允许使用 `out/0.5.0/staging-fixture/` 或 `%LOCALAPPDATA%\Programs\ChatGPT-Fix\staging\` 下的专用测试目录；不得把真实用户目录中的 staging 内容提交到 Git。

**禁止动作：**

- 不运行 `scripts/chatgpt-fix-doctor.ps1`，不读取/修改 `.codex`、`CODEX_HOME`、package `LocalState`、auth/token/SQLite/MCP 配置。
- 不执行 `Get-Process`、`Get-CimInstance` 或任何 process tree/command line/handle 查询；不启动/终止/结束任何进程，包括 ChatGPT 与 package primary EXE。
- 不执行 `Add/Remove-AppxPackage`，不写 `WindowsApps`，不创建/修改 shortcut，不修改 `current.json` 或任何 pointer/config 文件。
- 不下载 crate/toolchain/component，不访问网络，不写用户 Cargo/rustup cache，不运行安装器，不提权，不签名，不发布。
- staging 失败只进入 quarantine（`staging\.quarantine\` 或同级隔离目录），不得自动删除；不自动重试。

**Core 契约：**

- 新增 `chatgpt_fix.staging.v1` typed contract：`source_package_full_name`、`source_version`、`source_hash_manifest`、`staging_root`、`baseline_root`（目标 immutable 目录）、`files_staged`、`total_bytes`、`state`（`staged`/`verified`/`quarantined`）、`created_at_utc`。
- 新增 `stage`/`verify` CLI：`ChatGPT-Fix-Packer stage --probe-json <path> --out <staging-root>` 与 `ChatGPT-Fix-Packer verify --staging <path>`；只接受 fixture/staging 路径，live package 路径必须明确错误退出。
- 复制逻辑使用 P2 已冻结的 `LiveInspectionV1`/`BaselineV2` 数据；只复制 manifest 声明的 app 资源子集（白名单），拒绝 symlink/reparse、越界路径。
- `verify` 重新计算 staged 文件 SHA-256 并与 `source_hash_manifest` 对比；任何 mismatch 即 fail closed，不发布为 baseline。
- 最终 immutable baseline 目录创建后写入 `state.json`（schema `chatgpt_fix.baseline.v2`）；不创建/修改 current pointer。

**CLI 冻结：**

```text
ChatGPT-Fix-Packer stage --probe-json <path> --out <staging-root>   # 仅 A3 写集
ChatGPT-Fix-Packer verify --staging <path>                          # 仅 A3 写集
```

**离线门禁（与 P2 相同的环境变量）：**

```powershell
$env:RUSTUP_TOOLCHAIN = 'stable-x86_64-pc-windows-msvc'
$env:RUSTUP_NO_UPDATE_CHECK = '1'
$env:CARGO_NET_OFFLINE = 'true'
$env:CARGO_HOME = Join-Path $ChildRoot 'out\0.5.0\cargo-home'
$env:CARGO_TARGET_DIR = Join-Path $ChildRoot 'out\0.5.0\cargo-target'

cargo metadata --offline --locked --no-deps --format-version 1
cargo fmt --all -- --check
cargo clippy --offline --locked --workspace --all-targets -- -D warnings
cargo test --offline --locked --workspace
cargo build --offline --locked --workspace --release --target x86_64-pc-windows-msvc
git -C $ChildRoot diff --check
```

PowerShell AST 门禁与 P2 相同（禁止 mutation/process/network cmdlet）；Rust product scan 必须证明新增 `stage`/`verify` 的写路径只到达允许的 staging/baseline 根目录。

**失败补偿：** staging 写路径失败时保留 `staging\.quarantine\` 诊断与 receipt，标记 quarantine；不自动删除、不重试其他 package、不终止进程、不修改 shortcut。tracked 代码问题走新 commit 修复并重过门下/刑部；授权边界问题回到用户，不把 A3 升格为 A4。

**P3 完成条件：** 全部离线门禁 PASS；`stage`+`verify` 在 synthetic fixture 上端到端验证 PASS；A3 receipt 记录实际命令与全部 exclusion；P4 详细卡已在同一 P3 Goal 内追加并通过门下/刑部且 A4a（或 A4b）已明确授权；然后才运行版本接受事务、创建 `release/0.5.0`/`work/p4-launch-envelope`、P3->P4 handoff。未取得 A4 时 P3 Goal 保持 active。

### P4 / `release/0.6.0`

**状态：** `VERIFYING / AWAITING_ACCEPTANCE_TRANSACTION`。A4a 已由用户明确授予；P4 离线门禁（metadata/fmt/clippy/test 160/build）全 PASS；PowerShell AST 门禁 PASS；generation transaction 端到端验证 PASS（activate→pointer 原子切换、rollback→pointer 恢复 null、unverified baseline 拒绝、smoke_started 标记）；P4 build evidence（0.6.0 typed receipts）已提交；P5 详细卡已追加。下一步：运行第 4 节接受事务创建 `release/0.6.0`，随后进入 RELEASE-BRANCHED / HANDED-OFF。

**Goal：** 在 A4a 明确授权后，Manager 实现 generation transaction：把 `current.json` pointer 原子切换到已验证 baseline、备份 shortcut 完整元数据、受控 smoke 启动隔离 ChatGPT 并观察。P4 详细执行卡必须在首次真实启动前二选一：证明并测试"创建时原子加入 Job、失败不产生未归属 child"的 Windows 路径；或另获 A4b 后使用 suspended 两步路径。

**分支与版本：** `work/p4-launch-envelope` -> `release/0.6.0`；workspace/package/build 输出版本固定 `0.6.0`。P4 task 从 P3 accepted commit 预建分支 materialize，不得复用 P3 checkout。

**P4 唯一可消费授权：** `A0 + A1 + A2-P2（已有）+ A4a`（若走 suspended 两步路径则另需 A4b）。A4a 只授权：写 `%LOCALAPPDATA%\Programs\ChatGPT-Fix\current.json`（原子替换）、备份/恢复 `ChatGPT.lnk` 完整元数据（COM TargetPath/Arguments/WorkingDirectory/IconLocation，不改 WindowsApps）、在 A3 已建 immutable baseline 上启动隔离 ChatGPT 并做受控 smoke；A4a 不授权终止任何进程、不授权旧 baseline GC、不授权修改 WindowsApps/配置/`.codex`。

**tracked-content 写集：**

```text
Cargo.toml
Cargo.lock（仅 workspace version 0.5.0 -> 0.6.0 机械变化；新增 package/dependency 停止并请求 A1n）
src/chatgpt-fix-core/
src/chatgpt-fix-packer/（仅允许版本继承，不新增 packer 行为）
src/chatgpt-fix-manager/
scripts/chatgpt-fix-p2-probe.ps1（保持 P2 冻结内容）
tests/launch-fixtures/
docs/chatgpt-fix-doctor-v2-proposal.md（仅允许 P4 修正）
records/builds/0.6.0-win-x64/
checksums/0.6.0-win-x64.sha256
计划书.md
执行书.md
VERSION（仅接受事务）
```

`src/chatgpt-fix-launcher/` 仅允许 workspace version 继承使 `--version` 变为 `0.6.0`；不得新增 launcher 行为（P5 才扩展 ownership）。

**task-local/ignored 写集：** `out/0.6.0/**`、`.repo-control` 本 task state/event、`%TEMP%\ChatGPT-Fix-P4-acceptance-journal.json` 与冻结 handoff、`%LOCALAPPDATA%\Programs\ChatGPT-Fix\` 下 A4a 授权的 pointer/shortcut backup/smoke 目录。真实 `current.json` 切换只允许在 synthetic smoke fixture 或用户明确批准的 canary 上执行；不得把真实用户配置提交到 Git。

**禁止动作：**

- 不读取/修改 `.codex`、`CODEX_HOME`、package `LocalState`、auth/token/SQLite/MCP 配置。
- 不执行 `Get-Process`/`Get-CimInstance` 或 process tree/command line/handle 查询（除 P4 卡内明示的 smoke 观察外）；除受控 smoke 外不启动 ChatGPT；除 A4b 的从未 resume 单 child 失败补偿外不终止任何进程。
- 不执行 `Add/Remove-AppxPackage`，不写 `WindowsApps`，不修改 `ChatGPT.lnk` 之外的其他 shortcut，不改配置。
- 不下载 crate/toolchain/component，不访问网络，不写用户 Cargo/rustup cache，不运行安装器，不提权，不签名，不发布。
- 不执行旧 baseline GC、不删除 quarantine、不自动清理 staging。

**Core 契约：**

- 新增 `chatgpt_fix.generation.v1` typed contract：`generation_id`、`baseline_root`、`pointer_path`、`shortcut_backup`、`launch_ledger`、`state`（`prepared`/`activated`/`rolled_back`/`smoke_started`/`smoke_observed`）、`created_at_utc`。
- Manager `activate`/`rollback` 子命令：`ChatGPT-Fix-Manager activate --baseline <root> [--smoke]` 与 `ChatGPT-Fix-Manager rollback`；只接受 A3 已验证 baseline 目录，未验证 baseline 必须明确错误退出。
- `current.json` 切换使用同目录临时文件 + `fs::rename` 原子替换；失败不产生半写状态。
- shortcut 备份保存 COM 四元组到 `%LOCALAPPDATA%\Programs\ChatGPT-Fix\backups\<generation-id>\`；回滚时精确恢复。
- 若走 suspended 两步路径：先持久化 launch ledger（PID/创建时间/原始 process handle/launch nonce），在 resume 前完成 Job assignment；任一步失败都不得 resume；只有 `provisional-suspended-owned` 分类可依据 A4b 终止这一单个 child。

**CLI 冻结：**

```text
ChatGPT-Fix-Manager activate --baseline <root> [--smoke]   # 仅 A4a 写集
ChatGPT-Fix-Manager rollback                                # 仅 A4a 写集
```

**离线门禁（与 P3 相同的环境变量）：** metadata/fmt/clippy -D warnings/test/build --release --target x86_64-pc-windows-msvc 全 PASS；PowerShell AST 门禁同 P2/P3；Rust product scan 证明 `activate`/`rollback` 写路径只到达 A4a 允许目录。

**失败补偿：** pointer 切换失败保留备份与 generation receipt，标记 `rolled_back`，不自动重试；shortcut 恢复失败保留 backup 目录与 receipt；smoke 启动失败（非 A4b 场景）不终止任何进程，仅观察并记录。tracked 代码问题走新 commit 修复并重过门下/刑部；授权边界问题回到用户，不把 A4a 升格为 A5。

**P4 完成条件：** 全部离线门禁 PASS；generation transaction 在 synthetic smoke fixture 上端到端验证 PASS；A4a receipt 记录实际命令与全部 exclusion；P5 详细卡已在同一 P4 Goal 内追加并通过门下/刑部且 A4a 已明确授权；然后才运行版本接受事务、创建 `release/0.6.0`/`work/p5-ownership-envelope`、P4->P5 handoff。未取得 A4 时 P4 Goal 保持 active。

### P5 / `release/0.7.0`

**状态：** `VERIFYING / AWAITING_ACCEPTANCE_TRANSACTION`。A5 已由用户明确授予；P5 离线门禁（metadata/fmt/clippy/test 165/build）全 PASS；PowerShell AST 门禁 PASS；ownership observation 端到端验证 PASS（observe→report-only receipt、breakaway detection、outside_job 汇总）；P5 build evidence（0.7.0 typed receipts）已提交；P6 详细卡已追加。下一步：运行第 4 节接受事务创建 `release/0.7.0`，随后进入 RELEASE-BRANCHED / HANDED-OFF。

**Goal：** 在 P4 最小 ownership envelope（所有权包络）基础上，Launcher 保持到 ChatGPT 根进程退出，扩展 descendant Job membership、breakaway detection、ledger reconciliation 和退出报告。P5 只消费 A4a 的"启动并观察"范围；Job Object 不启用 kill-on-close，也不执行 graceful shutdown/terminate。除 A4b 的从未 resume 单 child 失败补偿外，任何动作性处理仍属于 A5/P6。

**分支与版本：** `work/p5-ownership-envelope` -> `release/0.7.0`；workspace/package/build 输出版本固定 `0.7.0`。P5 task 从 P4 accepted commit 预建分支 materialize，不得复用 P4 checkout。

**P5 唯一可消费授权：** `A0 + A1 + A2-P2 + A4a（已有）+ A5`（若需 suspended 两步路径则另需 A4b）。A5 只授权：创建 Job Object 并观察 owned tree 的 Job membership/breakaway/退出事件（report-only）；A5 不授权 graceful shutdown、Job close 或终止任何进程。

**tracked-content 写集：**

```text
Cargo.toml
Cargo.lock（仅 workspace version 0.6.0 -> 0.7.0 机械变化；新增 package/dependency 停止并请求 A1n）
src/chatgpt-fix-core/
src/chatgpt-fix-packer/（仅允许版本继承）
src/chatgpt-fix-manager/（仅允许版本继承与 P4 契约消费）
src/chatgpt-fix-launcher/
scripts/chatgpt-fix-p2-probe.ps1（保持 P2 冻结内容）
tests/ownership-fixtures/
docs/chatgpt-fix-doctor-v2-proposal.md（仅允许 P5 修正）
records/builds/0.7.0-win-x64/
checksums/0.7.0-win-x64.sha256
计划书.md
执行书.md
VERSION（仅接受事务）
```

**task-local/ignored 写集：** `out/0.7.0/**`、`.repo-control` 本 task state/event、`%TEMP%\ChatGPT-Fix-P5-acceptance-journal.json` 与冻结 handoff、`%LOCALAPPDATA%\Programs\ChatGPT-Fix\` 下 A4a 授权的 pointer/shortcut backup/smoke 目录。ownership 观察只允许在 synthetic process-tree fixture 上执行；不得把真实用户进程/会话提交到 Git。

**禁止动作：**

- 不读取/修改 `.codex`、`CODEX_HOME`、package `LocalState`、auth/token/SQLite/MCP 配置。
- 不执行 graceful shutdown、Job close 或 terminate；除 A4b 的从未 resume 单 child 失败补偿外不终止任何进程。
- 不执行 `Add/Remove-AppxPackage`，不写 `WindowsApps`，不修改 shortcut，不改配置。
- 不下载 crate/toolchain/component，不访问网络，不写用户 Cargo/rustup cache，不运行安装器，不提权，不签名，不发布。
- 不执行旧 baseline GC、不删除 quarantine、不自动清理 staging。

**Core 契约：**

- 新增 `chatgpt_fix.ownership.v1` typed contract：`launch_id`、`root_pid`、`job_members`、`breakaway_events`、`exit_report`、`state`（`observing`/`observed`/`reconciled`）、`created_at_utc`。
- Launcher 保持到根进程退出，记录 Job membership 与 breakaway detection；只观察不动作。
- ledger reconciliation：把 launch ledger 与实际观察到的 owned tree 对账，输出 report-only 差异。
- P5 卡内明确：Job Object 不启用 kill-on-close；若使用 suspended 两步路径，resume 前必须完成 Job assignment 且任一步失败不得 resume。

**CLI 冻结：**

```text
ChatGPT-Fix-Launcher observe --fixture-root <path>   # 仅 A5 report-only 观察
```

**离线门禁（与 P4 相同的环境变量）：** metadata/fmt/clippy -D warnings/test/build --release --target x86_64-pc-windows-msvc 全 PASS；PowerShell AST 门禁同 P2-P4；Rust product scan 证明 observe 只读、无 terminate/kill/Job close。

**失败补偿：** 观察中断保留 ownership receipt 与差异报告；不自动重试、不终止进程。tracked 代码问题走新 commit 修复并重过门下/刑部；授权边界问题回到用户，不把 A5 升格为 A6。

**P5 完成条件：** 全部离线门禁 PASS；ownership observation 在 synthetic process-tree fixture 上端到端验证 PASS；A5 receipt 记录实际命令与全部 exclusion；P6 详细卡已在同一 P5 Goal 内追加并通过门下/刑部且 A5 已明确授权；然后才运行版本接受事务、创建 `release/0.7.0`/`work/p6-owned-shutdown`、P5->P6 handoff。未取得 A5 时 P5 Goal 保持 active。

### P6 / `release/0.8.0`

**状态：** `DRAFT / AWAITING_AUTHORIZATION`。本卡由 P5 Goal 内以 A0 治理写集追加；收到用户明确绑定式 `AUTHORIZE_A5_OWNED_SHUTDOWN`（或等效确认）文本前，不得创建 P6 Goal/task，不得执行 graceful shutdown 或 Job close。

**Goal：** 在 A5 明确授权后，先用 synthetic process tree 验证 graceful shutdown、stdout/stderr handle inheritance、breakaway、PID reuse，再对 canary owned tree 进行受控 Job close。任何 suspected/excluded 命中即验收失败。

**分支与版本：** `work/p6-owned-shutdown` -> `release/0.8.0`；workspace/package/build 输出版本固定 `0.8.0`。P6 task 从 P5 accepted commit 预建分支 materialize，不得复用 P5 checkout。

**P6 唯一可消费授权：** `A0 + A1 + A2-P2 + A4a + A5（已有）+ A5-owned-shutdown`（即 A5 的 shutdown/Job close 子范围；若需 suspended 路径则另需 A4b）。A5 在本阶段扩展为：对 synthetic/canary owned tree 执行受控 graceful shutdown 与 Job close（仅限明确 owned 且已 reconciliation 的 tree）；仍不授权终止已 breakaway、excluded 或非 owned 进程。

**tracked-content 写集：**

```text
Cargo.toml
Cargo.lock（仅 workspace version 0.7.0 -> 0.8.0 机械变化；新增 package/dependency 停止并请求 A1n）
src/chatgpt-fix-core/
src/chatgpt-fix-packer/（仅允许版本继承）
src/chatgpt-fix-manager/（仅允许版本继承与契约消费）
src/chatgpt-fix-launcher/
scripts/chatgpt-fix-p2-probe.ps1（保持 P2 冻结内容）
tests/shutdown-fixtures/
docs/chatgpt-fix-doctor-v2-proposal.md（仅允许 P6 修正）
records/builds/0.8.0-win-x64/
checksums/0.8.0-win-x64.sha256
计划书.md
执行书.md
VERSION（仅接受事务）
```

**task-local/ignored 写集：** `out/0.8.0/**`、`.repo-control` 本 task state/event、`%TEMP%\ChatGPT-Fix-P6-acceptance-journal.json` 与冻结 handoff、`%LOCALAPPDATA%\Programs\ChatGPT-Fix\` 下 A4a 授权的 pointer/shortcut backup/smoke 目录。shutdown 测试只允许在 synthetic process-tree fixture 或用户明确批准的 canary owned tree 上执行。

**禁止动作：**

- 不读取/修改 `.codex`、`CODEX_HOME`、package `LocalState`、auth/token/SQLite/MCP 配置。
- 不终止已 breakaway、excluded、非 owned 或 reconciliation 失败标注 suspected 的进程。
- 不执行 `Add/Remove-AppxPackage`，不写 `WindowsApps`，不修改 shortcut，不改配置。
- 不下载 crate/toolchain/component，不访问网络，不写用户 Cargo/rustup cache，不运行安装器，不提权，不签名，不发布。
- 不执行旧 baseline GC、不删除 quarantine、不自动清理 staging。

**Core 契约：**

- 新增 `chatgpt_fix.shutdown.v1` typed contract：`launch_id`、`root_pid`、`shutdown_mode`（`graceful`/`job_close`）、`handled_pids`、`excluded_pids`、`suspected_pids`、`state`（`prepared`/`shutdown`/`closed`/`failed`）、`created_at_utc`。
- Launcher `shutdown --fixture-root <path>`：先 synthetic 验证 graceful shutdown、stdout/stderr handle inheritance、breakaway、PID reuse；再对 canary owned tree 受控 Job close。
- 任何 suspected/excluded 命中即验收失败（fail closed），不继续。

**CLI 冻结：**

```text
ChatGPT-Fix-Launcher shutdown --fixture-root <path>   # 仅 A5-owned-shutdown 范围
```

**离线门禁（与 P5 相同的环境变量）：** metadata/fmt/clippy -D warnings/test/build --release --target x86_64-pc-windows-msvc 全 PASS；PowerShell AST 门禁同 P2-P5；Rust product scan 证明 shutdown 只处理明确 owned 且已 reconciliation 的 tree，且 excluded/suspected 命中即停止。

**失败补偿：** shutdown 失败保留 shutdown receipt 与差异报告；不自动重试、不扩大处理范围。tracked 代码问题走新 commit 修复并重过门下/刑部；授权边界问题回到用户，不把 A5 升格为 A6。

**P6 完成条件：** 全部离线门禁 PASS；graceful shutdown/Job close 在 synthetic fixture 上端到端验证 PASS（含 handle inheritance、breakaway、PID reuse 负例）；A5-owned-shutdown receipt 记录实际命令与全部 exclusion；P7 详细卡已在同一 P6 Goal 内追加并通过门下/刑部且 A6 已明确授权；然后才运行版本接受事务、创建 `release/0.8.0`/`work/p7-effective-config`、P6->P7 handoff。未取得 A5-owned-shutdown 时 P6 Goal 保持 active。

### P7 / `release/0.9.0`

在 A6 明确授权后，Manager 生成 MCP/CODEX_HOME/history effective-config proposal，使用空 canary profile 验证，不复制 auth。每项 apply 都有独立 backup、receipt 和 rollback。

### P8 / `release/1.0.0`

集成 Packer/Manager/Launcher，生成 maintenance dry-run、SBOM、hash、build receipt 和 local release bundle；dry-run 不读取或修改超出只读卡范围的 `.codex` 内容，也不消费 A7。P8 同时产品化 NTC-NATIVE-20260801 的 app.asar preload/resource manifest、helper health/reapply、repair receipt 和 UI canary，但不把一次性手工 patch 当作稳定安装状态。`ChatGPT-Fix-Setup.exe` 的源码/本地构建可在不安装第三方系统 packer时进行；运行 Setup 需 A8a，安装系统级 packer/提权需 A8b，证书/签名需 A8c，三者不得合并授权。构建 Setup 不等于授权执行 Setup。实际 `.codex` 维护和外部发布仍禁止。

## 9. Handoff 执行程序

下一阶段 child branch、release branch、current-work/release/next-work 三个 root technical refs、`sync-branches` branch-sync events 与 doctor 已在第 4 节的单一接受事务中完成；其中 current-work 保持 task-start/preimage OID，release/next-work 指向新 governance OID。本节不得重复创建 ref。重跑 handoff 只能校验已有 branch/ref/OID/event，不得移动它们。

临时 handoff 文件固定为：

```text
%TEMP%\ChatGPT-Fix-P<N>-to-P<N+1>-handoff.md
```

handoff 只包含：两书修订、当前 Goal 状态、child/root refs 与 OID、版本、测试、授权消费、仓库外写入、rollback、下一任务命令、阻塞项和随机 nonce。文件完成后计算 SHA-256 并冻结；真实 source thread ID 可在该临时文件中短期保存，真实 successor ID 不回写文件，避免破坏 hash。

下一任务创建前，旧任务先调用 host `list_projects`，选取返回结果中 path/projectId 精确为 `D:\project` 的项目；再调用 `list_threads` 检查同技术 ref 的兼容 user-owned task。若存在候选，必须先用 `read_thread` 验证其 thread ID、技术 ref、task mount 和阶段状态，再发送握手 prompt；禁止重复 materialize。若无可复用任务，依据用户 2026-07-31 的 standing instruction 向 Codex host 提交 `create_thread` 请求。该 host 动作不是 court 的 `root -> root` 派遣，请求必须同时包含 `prompt` 与下列 target：

```json
{
  "type": "project",
  "projectId": "<list_projects 返回的 D:\\project projectId>",
  "environment": {
    "type": "worktree",
    "startingState": {
      "type": "branch",
      "branchName": "codex/ChatGPT-Fix/<next-work-branch>"
    }
  }
}
```

prompt 至少包含：source thread ID、临时 handoff 绝对路径/nonce/SHA-256、next technical ref/root OID、accepted child OID、唯一 task ID、只允许 materialize/read/verify、禁止创建 Goal/修改产品文件/提交，以及 ACK 命令。语义模板：

```text
你是由用户明确要求 Codex host 创建的下一阶段 user-owned root task，不是 source root 派出的 agent。
source_thread_id=<source-thread-id>
handoff=<absolute-temp-handoff>
handoff_nonce=<random-nonce>
handoff_sha256=<sha256>
root_ref=codex/ChatGPT-Fix/<next-work-branch>
root_oid=<root-governance-oid>
child_oid=<accepted-child-oid>
只执行 materialize-current、读取 workspace/child AGENTS/两书/handoff 和只读验证；不得 create_goal、修改产品文件或提交。
完成后重新计算 handoff SHA-256，并用 send_message_to_thread(source_thread_id, "HANDOFF_ACCEPTED nonce=<nonce> handoff_sha256=<sha256> root_ref=<ref> root_oid=<oid> child_oid=<oid>") 回传并停止，等待 START_GOAL。
```

不得省略 `prompt`/`startingState`，不得把未经 `list_projects` 验证的字面路径当作 projectId。host 返回 `threadId` 时直接记录；只返回 `clientThreadId` 时，用 `list_threads` 按标题与 root ref 解析最终 thread ID，禁止据此重复创建。任务创建后设置人类标题，并在新任务中运行：

```powershell
python (Join-Path $TaskRoot 'repo-control\repo_control.py') materialize-current `
  --root-branch codex/ChatGPT-Fix/<next-work-branch> `
  --task chatgpt-fix-p<N+1>-<slug>-<YYYYMMDDHHMMSS> `
  --attach-current
```

新任务验证 task-shell `workspace.yaml` 中 ChatGPT-Fix `version.current`、root ref/OID、attached child OID、根/子 index 与 handoff SHA-256 全部匹配后，通过唯一 ACK 通道回传绑定式 `HANDOFF_ACCEPTED`。旧任务必须对 create/list 得到的预期 thread ID 使用 `read_thread`，同时验证 ACK 字段、task mount 与阶段状态；超时或 host setup 未完成时保持当前 Goal active，只复查既有 task，不创建重复 task。

ACK 验证后，旧任务计算 `start_key = SHA256(handoff_nonce | next_phase | root_oid | child_oid)`，调用 `update_goal(status=complete)`，再发送 `START_GOAL phase=<P...> start_key=<hash>`。若发送失败或未收到回执，旧任务可在不修改文件/refs的前提下重发同一 key。新任务首次收到匹配 key 时创建唯一 Goal并回报 `GOAL_STARTED start_key=<hash>`；重复 key 只重发回执，不再创建 Goal；不匹配 key 拒绝。旧任务收到匹配 `GOAL_STARTED` 后停止协调。真实 successor ID 只保存在 host 状态/临时参数，不回写 tracked receipt或已冻结 handoff。

## 10. Court/Qoder/squad 执行规则

- 每个 visible phase task 是用户 standing instruction 下由 Codex host 创建的 user-owned root 协调 shell，不是 source root 的 agent child；host task creation 的 court dispatcher 为 none。
- 实现路径为 root -> 尚书 -> 对应六部；规格审查为 root -> 门下；计划/资料归纳为 root -> 中书。所有 spawn/follow-up/reuse/wake/create 记录 direct superior。
- user/host 创建 task 的授权不构成 root->root court 例外，也不扩大新 task 内部的派遣权限；若缺少明确用户授权，当前任务必须停下请求用户创建或授权 host 创建。
- 违规直派结果进入 quarantine，不得提交或进入 release；必须经正确上级重新派遣、复核和接受。
- QoderCLI/squad/Hermes 只用于 A0q read-only 审查/资料/评估，禁用工具和 session persistence；每个详细执行卡必须列出附件 allowlist。P0/P1 已批准附件为 `计划书.md`、`执行书.md`；辅助知识源为 `D:\project\.qoder\repowiki`（只读参考，不构成执行授权）；允许模型：QoderCLI `Qwen3.8-Max`、`DeepSeek-V4-Flash`、`DeepSeek-V4-Pro`；Hermes `provider=deepseek` 的 DeepSeek 模型（默认 `deepseek-v4-flash`）。Qoder/Hermes 旁路尽量经 squad 登记/编排；不得修改文件、运行 Git/doctor、创建 Goal、create_thread 或推进状态机。
- 向 QoderCLI/squad 提供附件前必须运行 secret/private-ID scan；Git receipt 不记录真实 Codex thread/client/task UUID 或 squad task UUID，只记录不可逆短标签。必要真实 ID 只可短期保存在 `%TEMP%` handoff。
- Qoder 输出必须由门下或中书按任务性质复核；冲突时以正式 court 审查为准。
- `.squad/` 和 `locks/*.receive.lock` 只能作为 ignored local coordination state；不得提交。`squad init` 自动添加到 `AGENTS.md`、`CLAUDE.md`、`GEMINI.md` 的通用说明不属于产品治理内容，验收前必须撤回。

## 11. 阶段回执模板

每阶段完成时复制本表到本节末尾并填写实际值；不得改写更早阶段回执。

| 字段 | 记录要求 |
|---|---|
| phase / goal | 阶段编号、Goal objective、Goal final status |
| thread | 标题与 handoff 文件名；真实 source/new thread ID 仅记录在 `%TEMP%` handoff，并由 host 工具解析 |
| child refs | work branch/release branch/accepted commit OID |
| root ref | technical ref 与 root OID |
| version | child VERSION 与 workspace current |
| tests | 完整命令、exit code、pass/fail 数 |
| reviews | 实现自审、规格审查、质量审查结论 |
| dispatch | court role、direct superior、dispatch path、quarantine 状态、Qoder/squad advisory 不可逆短标签；不得写真实 task UUID |
| authorizations | 消费的 A 编号和仍未授予编号 |
| external writes | 仓库外所有新增/修改路径；没有则明确 `none` |
| process/config/state | 启动、终止、配置、`.codex` 变化；没有则明确 `none` |
| rollback | 是否演练、结果、保留的上一状态 |
| repository state | root/child staged changes、既有 dirty noise 保留情况 |

## 12. P0 回执（accepted commit 输入）

| 字段 | 当前记录 |
|---|---|
| phase / goal | P0 / accepting / 两书与治理基线；本回执所在提交即 accepted commit，最终 OID 进入冻结 handoff |
| thread | `ChatGPT修复｜P0计划治理`；真实 thread ID 仅进入 `%TEMP%` handoff |
| child refs | `release/0.1.0@aa78b9f456de72aaffe214acf13293893ab71a96` -> candidate `work/p0-plan-governance@935c0c6bdabbdc0b71f52a2580c13650135dd65e`；accepted/release/next-work OID 由事务完成后写入冻结 handoff |
| root ref | `project/develop@62660c9b9327c4fba47a02502778421e95f08b55` 已形成唯一 ChatGPT-Fix 版本治理提交；current-work/release/next-work catalog refs 待 accepted commit 后原子建立 |
| version | child `VERSION=0.2.0`；root manifest `ChatGPT-Fix version.current=0.2.0` 已提交 |
| tests | pre-acceptance `repo-control doctor`: PASS；placeholder/secret/private-ID scan PASS；acceptance 与 P1 PowerShell parse PASS；root manifest worktree/index exact-hunk dry-check PASS；`git diff --check` PASS；refs/sync 后必须重跑 doctor |
| reviews | 中书只读结构审查完成；门下 rev-9 最终规格 PASS；尚书 rev-9 最终质量 PASS（无 minor）；QoderCLI no-tools/no-session advisory 为 PASS，不覆盖 court 结论 |
| dispatch | root -> 中书（结构）；root -> 门下（规格 PASS）；root -> 尚书 -> 工部/刑部（质量审查）；squad/Qoder advisory 标签 `p0-books-qoder-pass`，真实 task UUID 未记录 |
| authorizations | A0/A0q/A1 granted；A1n、A2、A3、A4a、A4b、A5-A7、A8a-A8c、A9 not granted |
| external writes | Codex Goal/标题元数据；`%TEMP%\ChatGPT-Fix-P0-acceptance-journal.json`（当前状态 `ROOT_GOVERNANCE_COMMITTED`，最终 hash 写入冻结 handoff）；repo 内 ignored `.squad/`、`locks/*.receive.lock`；QoderCLI 在 rev-4 隐私加固前读取过当时的两书附件，其中含随后移除的 task 类 UUID。调用已禁 tools/session persistence，但无法证明服务端零保留；此偏差以标签 `p0-qoder-preprivacy` 记录，不重复 UUID。产品目录 none |
| process/config/state | 仅按用户许可调用 QoderCLI 只读评估；未结束/清理进程，产品 process/config/`.codex` state none |
| rollback | P0 仅文档/Git refs；可回退工作分支 commit，不触碰产品状态 |
| repository state | child candidate 后 clean；root index clean；root 既有 dirty/untracked 原样保留且未进入治理提交 |

## 13. P1 回执（candidate acceptance 输入）

| 字段 | 当前记录 |
|---|---|
| phase / goal | P1 / active -> verifying / 在不读取 live package/process/`.codex` 的条件下建立 Rust workspace、schema、fixtures 和三个可执行程序的离线 dry-run；产品与构建证据完成。2026-08-06 恢复轮：A0q 扩展（repowiki/QoderCLI/Hermes/squad）与接受事务继续；接受事务完成后经 RELEASE-BRANCHED -> HANDED-OFF，最终状态与 OID 写入冻结 handoff |
| thread | `ChatGPT修复｜P1离线核心与EXE`；P1->P2 handoff 由 2026-08-06 恢复轮生成 |
| child refs | candidate preimage=`83fd63c8f2ce2d523aac12218204bc4647e5d47f`；candidate=`f75f0f458b0d8cc81874a22b1ba44924a2a1e16b`（仅 `VERSION` 0.2.0->0.3.0）；本回执所在提交将作为 accepted commit，accepted/release/next-work OID 写入冻结 handoff |
| root ref | task-shell/current-work technical ref `codex/ChatGPT-Fix/work/p1-offline-core@62660c9b9327c4fba47a02502778421e95f08b55` 保持 task-start identity（不可推进）；root governance=`c509c4e03e320fd691a6de12b69428d43d01680a`（manifest patch SHA-256=`45b73743c38838a4dca7da1a632524fe8cba0420769849307674357898ae3c25`）；release/next-work root refs 待接受事务原子创建 |
| version | Rust workspace/packages/build artifacts `0.3.0`；child `VERSION=0.2.0` 与 root manifest `version.current=0.2.0` 在 candidate/root governance 已精确更新为 `0.3.0`；acceptance journal SHA-256=`5669DC8ED8F313556C30AF15A26B3EC243A8F92FB7142AA2C5F8E3A47F894402`（state=`ROOT_GOVERNANCE_COMMITTED`） |
| tests | task-local/offline `cargo metadata/fmt/clippy -D warnings/test/build --release --target x86_64-pc-windows-msvc` PASS；workspace 65/65 tests PASS；Packer/Manager canonical plan 字节一致；Launcher `would_start=false` 且 live launch exit 4；三个 release EXE size/SHA、typed receipts、Cargo.lock、license list 与 SPDX 5 packages/3 files 相互校验 PASS。2026-08-06 恢复轮：journal hash 复核一致；两书 secret/private-ID scan PASS；`git diff --check` PASS |
| artifacts | ignored `out/0.3.0/win-x64/ChatGPT-Fix-Launcher.exe` 246272 bytes / `8453683bf51ff14f1128f17afa99359a635dc82ef296b03f340237c255260ff3`；Manager 233472 / `931a40f845833290bfb4be349247d727a75e57132813d27fea368db1d7c81cad`；Packer 233984 / `58d1a545ebea362d257220443f4fda680b96e7bf427c7918e9a90a3cf8895c66`；unsigned；binary source commit `ad6ff3b22db1f3c9405f771a99c1dd90bc5db9eb` |
| reviews | Task 1-3 已完成实现自审、门下规格与刑部质量循环；Task 3 的 `not_fixture_root` 阻断项修复后复审 PASS。2026-08-01 恢复轮门下 build-evidence 子审查与三文件规格审查 PASS；尚书质量审查曾因两书顶部控制面仍为 P0、缺 task-shell index gate、journal guard 顺序和 delegated external-state 表述给出 BLOCK，当前四项已修复且门下/尚书复审 PASS。2026-08-06 恢复轮：宿主子代理消息投递失败，三省/工部职责按 `serial_inline` 由主线执行并记录 `host_dispatch_delivery_failed`；A0q 扩展与接受事务按两书逐项执行。QoderCLI(Qwen3.8-Max) 只读 advisory（有条件 PASS）三项建议已处理：doctor-skip 偏差入 deviation/journal（见 `P1-A1-RO-004`）、§1 状态统一为 VERIFYING、计划书 rev-11 审查状态回填 |
| dispatch | 已完成路径：root -> 尚书省 -> 工部 -> fresh workshop（Task 1-3 implementation）；root -> 门下省（Task 1-3 spec）；root -> 尚书省 -> 刑部（Task 1-3 quality）。2026-08-06 恢复轮因宿主 spawn/followup 消息投递失败改 `serial_inline`（reason=`host_dispatch_delivery_failed`）；QoderCLI(Qwen3.8-Max) advisory 短标签 `p1-recovery-a0q-qoder-20260806`：有条件 PASS，三项建议已全部处理并采纳；Hermes(deepseek-v4-flash) advisory 超时未完成（短标签 `p1-recovery-a0q-hermes-timeout-20260806`），恢复轮未重跑 |
| authorizations | 消费 A0/A1；A0q 于 2026-08-06 扩展（repowiki/QoderCLI `Qwen3.8-Max`+DeepSeek 系列/Hermes DeepSeek/squad 编排）；A10-NTC 仅消费 delegated evidence 登记范围；A1n、广义 A2、A3、A4a、A4b、A5-A7、A8a-A8c、A9 均未授予或消费；A2-P2 详细子门已定义但仍未授予；native overlay 重打包、helper task 变更、重启和 UI live proof 未消费 |
| deviation | `P1-A1-RO-001`：A2 未授予时误执行一次 `Get-Process cargo,rustc` 只读 probe；返回无 process record，未启动/结束/清理进程，未修改配置或状态。`P1-A1-RO-002`：本轮状态核对再次误带同一只读 probe；同样无 process record，未启动/结束/清理进程，未读取 package/config/`.codex` 或修改状态；此后停止所有 process probe。两项均不解释为 A2 授权，用户已要求记录偏差并按原 A1 继续。`P1-A1-RO-003`（2026-08-06）：宿主 spawn/followup 消息投递失败（探针验证无 payload），fork_turns=1 探针基于上下文误改 decretum-matrix beta106 执行书并创建 Qoder skills junction 与 root `.squad` 状态；全部已隔离回退（beta106 执行书 §3.1 移除、junction 删除、`.squad` 清理），本轮改 `serial_inline` 并记录。`P1-A1-RO-004`（2026-08-06）：按 P1 禁令跳过 `repo-control doctor` 与产品 doctor；接受事务脚本中 `VERIFIED(doctor=PASS)` 无法诚实满足，改为 journal 显式记录 `doctor=SKIPPED_P1_PROHIBITION` 并以本 receipt 偏差登记替代 doctor PASS；验收矩阵 Workspace 维度改以 refs 原子校验、branch-sync events 与 index 检查验证，不再依赖 doctor 输出。 |
| external writes | 本 P1 task：tracked `records/native-token-cost-integration-20260801.md`（normalized delegated evidence）、repo 内 ignored `out/0.3.0/cargo-home/`/`cargo-target/`/`win-x64/` 和 Codex Goal/task 元数据；未写 live WindowsApps/shortcut/config/`.codex`/用户 Rust cache/release package。另有外部会话委托登记但本 P1 task 未独立复核或重复执行的写入：`%USERPROFILE%\.codex\backups\codex-token-cost-native-20260801-074411`、launcher-owned baseline 的 `app.asar`/`native-token-cost` resources 与 Scheduled Task `CodexTokenCostHelper`；详见 native receipt，不计作本 P1 task 写入或 A2 授权。 |
| process/config/state | 本 P1 task 仅运行 task-local rustc/cargo/tests 与三个 CLI 的 `--version`、fixture plan/review/dry-run/live-refusal smoke；CLI 自然退出且未启动 ChatGPT。除已记录两次无结果只读 process probe 偏差外，本 task 未读 live package/process/config/`.codex`，也未终止/清理进程或修改配置、task/system state。外部会话另报告补丁后已有 ChatGPT 会话和 helper health，但 P1 未独立 reread、restart 或 mutate；renderer UI proof 仍 `PENDING`，详见 native receipt。 |
| rollback | P1 只有 Git commits 与 ignored task-local build outputs，没有 live 系统事务；失败时保留 evidence/quarantine，不自动删除或触碰用户状态 |
| repository state | 2026-08-06 恢复轮现场：child worktree clean at `f75f0f4`；canonical child `release/0.2.0` clean；root 既有 dirty/untracked（`.gitignore`、`workspace.yaml` 无关改动、`docs/` 等）原样保留且不进入本阶段提交；本回执所在提交只 stage `计划书.md`、`执行书.md`；P1 禁 doctor，`repo-control doctor` 与产品 doctor 均跳过并在本 receipt 记录 |

## 14. P2 回执（accepted commit 输入）

| 字段 | 当前记录 |
|---|---|
| phase / goal | P2 / verifying -> accepting / 让 Packer 成为 OpenAI.Codex 当前用户包唯一 canonical inspect/plan 生成者；Manager 只解析校验原样展示；doctor v2 源码提案不运行 doctor；本回执所在提交将作为 accepted commit |
| thread | ChatGPT修复｜P2实时只读发现；真实 thread ID 仅进入 %TEMP% handoff |
| child refs | candidate preimage=e8be93f29c7ec22f85c31b5ad7eff9f266f70a04；candidate=b7c117f58d7b5875f781fe14ca501fd1b87b4a7b（含 PS7 兼容修复、doctor v2 proposal、P3 详细卡、A2-P2 receipt、两书授权记录）；本回执所在提交将作为 accepted commit，accepted/release/next-work OID 写入冻结 handoff |
| root ref | task-shell/current-work technical ref codex/ChatGPT-Fix/work/p2-live-inspection@c509c4e03e320fd691a6de12b69428d43d01680a 保持 task-start identity（不可推进）；root governance=45e83ae69e07a35f5f860316989e25adcafd8c00（manifest patch SHA-256=see acceptance journal manifest_patch_sha256）；release/next-work root refs 待接受事务原子创建 |
| version | Rust workspace/packages/build artifacts 0.4.0；child VERSION=0.4.0 与 root manifest ersion.current=0.4.0 已在接受事务精确更新；acceptance journal 见 %TEMP% |
| tests | task-local/offline cargo metadata/fmt/clippy -D warnings/test/build --release --target x86_64-pc-windows-msvc 全 PASS；workspace 141/141 tests PASS（含 build_evidence）；PowerShell AST 门禁 PASS；Packer/Manager canonical plan 字节一致；live inspect/plan/review exit 0、字节一致；三个 release EXE size/SHA、typed receipts、Cargo.lock、license/SPDX 相互校验 PASS |
| artifacts | ignored out/0.4.0/win-x64/ChatGPT-Fix-Launcher.exe 246784 / 8930bb38b06287c0fd08d77c5a137634ebf2ef8cc845e4ab68e6808eed86c16d；Manager 285184 / 409ca63b4cfdc4a4f026eecc3dd88e42fc4f9805ff70f469f40632ce403a7540；Packer 417792 / 840e92350d037ffacfcb979108a8fa3f2906f8f1bd03370a079b9502a8966da5；unsigned；source commit 873b880f98640e40cc43baeb8d4ea1d0a78b0eac |
| reviews | P2 实现已按 serial_inline 完成自审；门下规格审查与刑部质量审查按 host_dispatch_delivery_failed 降级为 serial_inline 复核并记录；P3 详细卡随本阶段 A0 治理写集追加并复核 |
| dispatch | 宿主子代理消息投递不可用，沿用 serial_inline（reason=host_dispatch_delivery_failed，P1-A1-RO-003 记录）；Qoder/Hermes advisory 本轮未消费 |
| authorizations | 消费 A0/A1/A2-P2；A3 已由用户于 P2 收尾明确授予（P3 staging 写集）；A1n、广义 A2、A4a、A4b、A5-A7、A8a-A8c、A9 均未授予或消费 |
| external writes | 本 P2 task：tracked docs/chatgpt-fix-doctor-v2-proposal.md、records/builds/0.4.0-win-x64/A2-P2-live-receipt.txt、两书；repo 内 ignored out/0.4.0/**（含 live-evidence 只读 JSON）；%TEMP% journal/handoff；未写 live WindowsApps/shortcut/config/.codex/用户 Rust cache |
| process/config/state | 仅运行 task-local rustc/cargo/tests 与三个 CLI 的 fixture/live-readonly/review smoke（A2-P2 授权范围）；probe 只读，未启动/终止任何进程，未读 .codex/process/LocalState，未改配置 |
| rollback | P2 只有 Git commits/refs 与 ignored task-local build outputs，无 live 系统事务；失败保留 evidence/quarantine，不自动删除 |
| repository state | candidate 前 child index clean；root index clean；canonical child 仅 docs/HANDOFF.md 未提交（用户指令例外，含真实 UUID，禁止提交）；root 既有 dirty/untracked 原样保留未进入任何提交 |


### 14.1 P2 回执修正附注（2026-08-06，append-only）

§14 由接受事务脚本自动生成时因 here-string 转义缺陷，占位符未展开且出现 NUL/CR 字节；本节记录真实值，§14 字段以本节为准。真实 source thread ID 仅存 %TEMP% handoff，不写入 Git。

| 字段 | 真实值 |
|---|---|
| child preimage OID | e8be93f29c7ec22f85c31b5ad7eff9f266f70a04（P2 接受事务前 child HEAD） |
| candidate OID | b7c117f58d7b5875f781fe14ca501fd1b87b4a7b（docs: record P2 candidate receipt） |
| accepted child OID | b6645c8a86a859dc17b1fccf9ebbef5842f4f0e3（docs: record P2 acceptance receipt，即 release/0.4.0） |
| root governance OID | 45e83ae69e07a35f5f860316989e25adcafd8c00（chore: accept ChatGPT Fix 0.4.0） |
| manifest patch SHA-256 | 见 %TEMP%\ChatGPT-Fix-P2-acceptance-journal.json manifest_patch_sha256 |
| release/next-work root refs | codex/ChatGPT-Fix/release/0.4.0=45e83ae、codex/ChatGPT-Fix/work/p3-baseline-packer=45e83ae、codex/ChatGPT-Fix/work/p2-live-inspection=c509c4e（task-start identity，不可推进） |
| journal | %TEMP%\ChatGPT-Fix-P2-acceptance-journal.json，state=VERIFIED，doctor=PASS_WITH_UNRELATED_DEVRETUM_ERROR（decretum-matrix 非独立 Git root 为既有 workspace 状态，与 P2 无关；ChatGPT-Fix 项目 check 全 PASS） |


### 14.2 P2 handoff 冻结记录（2026-08-06，append-only）

- P2->P3 handoff 文件：%TEMP%\ChatGPT-Fix-P2-to-P3-handoff-20260806.md
- handoff SHA-256：45b15cdffa4bd2fd5253585895092d789f8723e4a19043b3645adad8c9c1c6ba
- handoff nonce：NONCE_9F3B2D77C1A84E0562FD（绑定 handoff SHA-256）
- acceptance journal：%TEMP%\ChatGPT-Fix-P2-acceptance-journal.json，state=VERIFIED
- release/0.4.0 = 197de08315abbb5531ab49d9b59e774c7ded4870（含 §14.1 修正附注）
- root governance = 45e83ae69e07a35f5f860316989e25adcafd8c00
- 下一工作分支：work/p3-baseline-packer = 197de08315abbb5531ab49d9b59e774c7ded4870


## 15. P3 回执（accepted commit 输入）

| 字段 | 当前记录 |
|---|---|
| phase / goal | P3 / verifying -> accepting / Packer stage/verify immutable baseline staging；不写 pointer/shortcut/WindowsApps/config/.codex，不启动/终止进程 |
| thread | ChatGPT修复｜P3基线暂存；真实 thread ID 仅进入 %TEMP% handoff |
| child refs | candidate preimage=365202fd40ef3e7e072f7e3a39aad7e67f22f4b3；candidate=2fe6fe93e27a8ed5771ab8d63922a15ac7223488（含 staging 实现、0.5.0 build evidence、两书授权记录）；本回执所在提交将作为 accepted commit |
| root ref | current-work technical ref codex/ChatGPT-Fix/work/p3-baseline-packer@45e83ae69e07a35f5f860316989e25adcafd8c00 保持 task-start identity（不可推进）；root governance=b3724140870e7d664d38de365b295e09856abaf2（manifest patch SHA-256=a016958ebca706138479bf795cd0ee25a9c79f2339b7e485402a2216ed271b81） |
| version | Rust workspace/packages/build artifacts 0.5.0；child VERSION=0.5.0 与 root manifest version.current=0.5.0 已在接受事务精确更新；acceptance journal 见 %TEMP% |
| tests | task-local/offline cargo metadata/fmt/clippy -D warnings/test/build --release --target x86_64-pc-windows-msvc 全 PASS；workspace 148/148 tests PASS；PowerShell AST 门禁 PASS；stage/verify 端到端 PASS（stage→verified、白名单过滤、quarantine fail-closed、live WindowsApps 拒绝）；0.5.0 typed receipts 校验 PASS |
| artifacts | ignored out/0.5.0/win-x64/ChatGPT-Fix-Launcher.exe 246784 / 4235f8fb155af07266550bbcd6d4c7572bf99c06ebb07002a97fd9e7b5b90c05；Manager 284672 / d4c3ac921375463134d7f3550f5bbb76b166930ed24a6aeab4ca941e5c81ebb2；Packer 497664 / 8d7669fce33a410028577d6c18c6315444453c81125f7db4ab49fc971173674b；unsigned；source commit ba3365e7cca15ae15a8d6f19427cdff12cd3aefd |
| reviews | P3 实现已按 serial_inline 完成自审；门下规格审查与刑部质量审查按 host_dispatch_delivery_failed 降级为 serial_inline 复核并记录；P4 详细卡随本阶段 A0 治理写集追加并复核 |
| dispatch | 宿主子代理消息投递不可用，沿用 serial_inline（reason=host_dispatch_delivery_failed）；Qoder/Hermes advisory 本轮未消费 |
| authorizations | 消费 A0/A1/A2-P2/A3；A4a 已由用户于 P3 收尾明确授予（P4 generation transaction 写集）；A1n、广义 A2、A4b、A5-A7、A8a-A8c、A9 均未授予或消费 |
| external writes | 本 P3 task：tracked tests/staging-fixtures/、records/builds/0.5.0-win-x64/、checksums/0.5.0-win-x64.sha256、两书、.gitattributes（P2 遗留 sha256 行尾修复）；repo 内 ignored out/0.5.0/**；%TEMP% journal/handoff；未写 live WindowsApps/shortcut/config/.codex/用户 Rust cache |
| process/config/state | 仅运行 task-local rustc/cargo/tests 与 Packer stage/verify CLI（A3 授权范围，fixture 路径）；未启动/终止任何进程，未读 .codex/process/LocalState，未改配置 |
| rollback | P3 只有 Git commits/refs 与 ignored task-local build outputs，无 live 系统事务；staging 失败保留 quarantine，不自动删除 |
| repository state | candidate 前 child index clean；root index clean；canonical child 仅 docs/HANDOFF.md 未提交（用户指令例外，含真实 UUID，禁止提交）；root 既有 dirty/untracked 原样保留未进入任何提交 |


### 15.1 P3 handoff 冻结记录（2026-08-06，append-only）

- P3->P4 handoff 文件：%TEMP%\ChatGPT-Fix-P3-to-P4-handoff-20260806.md
- handoff SHA-256：a5464a0e2ac3d591446487eee2abbb582f77c8ca4548ed9a4eefc971c4feabae
- handoff nonce：NONCE_A4B2C8E1F60D3A9C5710（绑定 handoff SHA-256）
- acceptance journal：%TEMP%\ChatGPT-Fix-P3-acceptance-journal.json，state=VERIFIED
- release/0.5.0 = e1daf280193da093430d867d93c4714fd48a797a
- root governance = b3724140870e7d664d38de365b295e09856abaf2
- 下一工作分支：work/p4-launch-envelope = e1daf280193da093430d867d93c4714fd48a797a


## 16. P4 回执（accepted commit 输入）

| 字段 | 当前记录 |
|---|---|
| phase / goal | P4 / verifying -> accepting / Manager generation transaction：current.json 原子切换 + shortcut COM 元数据备份 + 受控 smoke 观察；不写 WindowsApps/config/.codex，不启动/终止任何进程（真实启动留待 P5 canary） |
| thread | ChatGPT修复｜P4激活事务；真实 thread ID 仅进入 %TEMP% handoff |
| child refs | candidate preimage=ffe09fc7e08feb31988a47cbffb256c4666c1341；candidate=0c0e64eabc47bf35f043941afe947dfb18053391（含 generation 实现、0.6.0 build evidence、两书授权记录）；本回执所在提交将作为 accepted commit |
| root ref | current-work technical ref codex/ChatGPT-Fix/work/p4-launch-envelope@b3724140870e7d664d38de365b295e09856abaf2 保持 task-start identity（不可推进）；root governance=53be5b00b2c14fbff1f11c43d28efc671b1d8e5a（manifest patch SHA-256=fb176fd8af44944556406b58705ce73ad84602100b493985a80e81a9635c48a0） |
| version | Rust workspace/packages/build artifacts 0.6.0；child VERSION=0.6.0 与 root manifest version.current=0.6.0 已在接受事务精确更新；acceptance journal 见 %TEMP% |
| tests | task-local/offline cargo metadata/fmt/clippy -D warnings/test/build --release --target x86_64-pc-windows-msvc 全 PASS；workspace 160/160 tests PASS；PowerShell AST 门禁 PASS；generation transaction 端到端 PASS（activate→pointer 原子切换、rollback→pointer 恢复 null、unverified baseline 拒绝、smoke_started 标记）；0.6.0 typed receipts 校验 PASS |
| artifacts | ignored out/0.6.0/win-x64/ChatGPT-Fix-Launcher.exe 246784 / 2cc357743588e36f306f9451f9e920ea7396e45305f40a2b905725c4bfde9f05；Manager 365056 / 0167b0b2ab5eaeb5eca5d4cf5032f41cfeecd63278abe8a35609f9fb105cfe26；Packer 496640 / 3a8774c88626b1d342e42ff97fbf04f885eb8cda655308f5dab2a26d2462a41c；unsigned；source commit ddba3c16a0188a5a21c14cad5320d653a827d50f |
| reviews | P4 实现已按 serial_inline 完成自审；门下规格审查与刑部质量审查按 host_dispatch_delivery_failed 降级为 serial_inline 复核并记录；P5 详细卡随本阶段 A0 治理写集追加并复核 |
| dispatch | 宿主子代理消息投递不可用，沿用 serial_inline（reason=host_dispatch_delivery_failed）；Qoder/Hermes advisory 本轮未消费 |
| authorizations | 消费 A0/A1/A2-P2/A3/A4a；A5 已由用户于 P4 收尾明确授予（P5 ownership 观察写集）；A1n、广义 A2、A4b、A6-A7、A8a-A8c、A9 均未授予或消费 |
| external writes | 本 P4 task：tracked tests/launch-fixtures/、records/builds/0.6.0-win-x64/、checksums/0.6.0-win-x64.sha256、两书；repo 内 ignored out/0.6.0/**；%TEMP% journal/handoff；未写 live WindowsApps/shortcut/config/.codex/用户 Rust cache |
| process/config/state | 仅运行 task-local rustc/cargo/tests 与 Manager activate/rollback CLI（A4a 授权范围，fixture 路径）；未启动/终止任何进程（smoke 仅标记 smoke_started，不真实 spawn），未读 .codex/process/LocalState，未改配置 |
| rollback | P4 只有 Git commits/refs 与 ignored task-local build outputs，无 live 系统事务；pointer 切换失败保留 backup 与 receipt，标记 rolled_back，不自动重试 |
| repository state | candidate 前 child index clean；root index clean；canonical child 仅 docs/HANDOFF.md 未提交（用户指令例外，含真实 UUID，禁止提交）；root 既有 dirty/untracked 原样保留未进入任何提交 |


### 16.1 P4 handoff 冻结记录（2026-08-06，append-only）

- P4->P5 handoff 文件：%TEMP%\ChatGPT-Fix-P4-to-P5-handoff-20260806.md
- handoff SHA-256：4fc7c4cd67e4ae14678269c115418f76bdd7d9c05af48f03860da85dcdefb98e
- handoff nonce：NONCE_B7D3E9F20A5C6148DE23（绑定 handoff SHA-256）
- acceptance journal：%TEMP%\ChatGPT-Fix-P4-acceptance-journal.json，state=VERIFIED
- release/0.6.0 = 02d1be7053c007988347fcabaff7d9ad6dc7715f
- root governance = 53be5b00b2c14fbff1f11c43d28efc671b1d8e5a
- 下一工作分支：work/p5-ownership-envelope = 02d1be7053c007988347fcabaff7d9ad6dc7715f


## 17. P5 回执（accepted commit 输入）

| 字段 | 当前记录 |
|---|---|
| phase / goal | P5 / verifying -> accepting / Launcher report-only ownership observation：Job membership/breakaway/退出事件只读观察；不执行 graceful shutdown、Job close 或 terminate（真实进程树观察留待 canary） |
| thread | ChatGPT修复｜P5所有权观察；真实 thread ID 仅进入 %TEMP% handoff |
| child refs | candidate preimage=1b5fb31abfc8eb5b841c10bae8b7ae00f3667a80；candidate=5678b0475507d44100d06ff87e19f5b6d3fb0e36（含 ownership 实现、0.7.0 build evidence、两书授权记录）；本回执所在提交将作为 accepted commit |
| root ref | current-work technical ref codex/ChatGPT-Fix/work/p5-ownership-envelope@53be5b00b2c14fbff1f11c43d28efc671b1d8e5a 保持 task-start identity（不可推进）；root governance=73efd29b782aed06d6c57bda1d5c45355c04e55c（manifest patch SHA-256=b7607142180d1ca517da6add4db6a2d8089ef50eb2a30c44d22ec3db3a66cb8e） |
| version | Rust workspace/packages/build artifacts 0.7.0；child VERSION=0.7.0 与 root manifest version.current=0.7.0 已在接受事务精确更新；acceptance journal 见 %TEMP% |
| tests | task-local/offline cargo metadata/fmt/clippy -D warnings/test/build --release --target x86_64-pc-windows-msvc 全 PASS；workspace 165/165 tests PASS；PowerShell AST 门禁 PASS；ownership observation 端到端 PASS（observe→report-only receipt、breakaway detection、outside_job 汇总）；0.7.0 typed receipts 校验 PASS |
| artifacts | ignored out/0.7.0/win-x64/ChatGPT-Fix-Launcher.exe 285696 / 5d4441630f0a938704f441d04ac23fb4c914476ac282ab26c4c0da09ab3b3596；Manager 364544 / 101ea9a1af0049f55211cafe5a4d98bbd0e5c0fd8c61886ace601269ec3df089；Packer 496640 / c78cc360a5800901631669c6c77a55a39bf8ca35d39d9113e0dd59f05d39ddb9；unsigned；source commit eb07a1dc808eeb1b07f2919d7fd25035d5ef88e6 |
| reviews | P5 实现已按 serial_inline 完成自审；门下规格审查与刑部质量审查按 host_dispatch_delivery_failed 降级为 serial_inline 复核并记录；P6 详细卡随本阶段 A0 治理写集追加并复核 |
| dispatch | 宿主子代理消息投递不可用，沿用 serial_inline（reason=host_dispatch_delivery_failed）；Qoder/Hermes advisory 本轮未消费 |
| authorizations | 消费 A0/A1/A2-P2/A3/A4a/A5；A5 已由用户于 P4 收尾明确授予（P5 ownership 观察写集）；A1n、广义 A2、A4b、A6-A7、A8a-A8c、A9 均未授予或消费 |
| external writes | 本 P5 task：tracked tests/ownership-fixtures/、records/builds/0.7.0-win-x64/、checksums/0.7.0-win-x64.sha256、两书；repo 内 ignored out/0.7.0/**；%TEMP% journal/handoff；未写 live WindowsApps/shortcut/config/.codex/用户 Rust cache |
| process/config/state | 仅运行 task-local rustc/cargo/tests 与 Launcher observe CLI（A5 授权范围，fixture 路径）；未启动/终止/关闭任何进程（observe 纯只读），未读 .codex/process/LocalState，未改配置 |
| rollback | P5 只有 Git commits/refs 与 ignored task-local build outputs，无 live 系统事务；观察中断保留 ownership receipt 与差异报告，不自动重试 |
| repository state | candidate 前 child index clean；root index clean；canonical child 仅 docs/HANDOFF.md 未提交（用户指令例外，含真实 UUID，禁止提交）；root 既有 dirty/untracked 原样保留未进入任何提交 |
