# ChatGPT-Fix 分阶段修复实施计划书

> **供阶段执行代理使用：** 每个阶段必须使用 `superpowers:subagent-driven-development` 或 `executing-plans` 逐任务执行，并依次完成规格符合性审查和代码质量审查。阶段只能在验收、版本分支和新任务交接全部完成后关闭 Goal。

**目标：** 在不修改官方 `WindowsApps` 包、不默认清理 `.codex`、不把独立 watchdog 作为主路径的前提下，逐阶段构建可审计、可回滚的 ChatGPT/Codex Windows 本地修复系统，并最终交付 Packer、Launcher、Manager 和可选 Setup EXE。

**架构：** 稳定入口由 `ChatGPT-Fix-Launcher.exe` 接管；`ChatGPT-Fix-Packer.exe` 只负责发现、规划、暂存和验证隔离 baseline；`ChatGPT-Fix-Manager.exe` 负责显式授权后的激活、回滚和配置提案。Launcher 在应用存活期内维护 launch ledger（启动账本）和 Windows Job Object（作业对象）归属，独立 watchdog 不进入主链路。

**技术栈：** 首选当前已安装的 Rust `1.97.1`、目标 `x86_64-pc-windows-msvc`、PowerShell 7/Windows PowerShell、现有 `repo-control`、Git。P1 必须提交 `rust-toolchain.toml` 和 `Cargo.lock`；不得因构建方便静默安装 .NET SDK、WiX、Inno Setup、NSIS、签名工具或其他系统级依赖。

---

## 1. 文档控制

| 项 | 值 |
|---|---|
| 文档状态 | ACTIVE，P1 离线核心与 EXE |
| 初始修订 | 2026-07-31 / rev-1 |
| 当前已接受产品版本 | `0.2.0` |
| 当前阶段目标版本 | `0.3.0` |
| 当前阶段工作分支 | `work/p1-offline-core` |
| 当前阶段 Goal | ChatGPT-Fix P1 离线核心、fixture 与首批 EXE |
| 权威范围 | 产品目标、永久边界、阶段/版本路线、交付物与验收标准 |

本文件回答“做什么、为什么、边界是什么”。[执行书.md](执行书.md) 回答“当前由谁、在哪个分支、按什么命令与证据执行”。

权威顺序为：当前用户明确指令、`D:\project\AGENTS.md`/`workspace.yaml`、`D:\project\ChatGPT-Fix\AGENTS.md`、本计划书、执行书、阶段临时 handoff、历史审查与交接材料。历史 [docs/HANDOFF.md](docs/HANDOFF.md) 和 [docs/修复升级执行方向报告.md](docs/修复升级执行方向报告.md) 保留为背景证据，不再单独授权执行。

## 2. 已确认事实与待证假设

### 2.1 已确认事实

- 归档快照中的注册官方包版本为 `26.721.4979.0`，现有 local baseline 来源为 `26.707.8479.0`，已存在版本漂移。
- 当前快捷方式直接指向旧 baseline 的 `ChatGPT.exe`，因此仅切换 `current.json` 不能完成真实回滚。
- 官方配置支持 `mcp_servers.<id>.enabled`、`history.persistence`、`history.max_bytes` 和 `CODEX_HOME` 状态根；`history.max_bytes` 只约束 history 文件，不约束 rollout JSONL 或 `logs_2.sqlite`。
- 现有 doctor 的 lifecycle candidate（生命周期候选）会包含“名称匹配且超过阈值、但未证明 Codex ownership（所有权）”的进程，不能作为终止清单。
- 本机当前有完整 Rust MSVC 工具链；只有 .NET runtime，没有 .NET SDK；未发现 WiX/Inno Setup/NSIS/signtool。

### 2.2 必须由阶段实验验证的假设

- Windows Desktop 是否完整继承 Launcher 设置的 `CODEX_HOME`。
- 最新注册 MSIX 内容复制到普通用户目录后是否仍能完整启动和工作。
- Electron/app-server 及其 MCP/工具后代是否全部保持在 Launcher 创建的 Job Object 内，是否存在 breakaway（脱离作业）路径。
- 包更新期间执行发现或复制时，能否可靠检测 TOCTOU（检查与使用之间状态变化）并失败关闭。
- Packer/Launcher/Manager 的本地 unsigned build（未签名构建）能否通过明确 hash、来源和用户信任门满足本地使用要求。

## 3. 永久安全不变量

1. 不写入、替换或 patch `C:\Program Files\WindowsApps\OpenAI.Codex_*`。
2. 不把官方 OpenAI 包内容嵌入 Git、Setup EXE 或可分发 release bundle。
3. Packer 默认是 stdout-only dry-run（仅标准输出演练）；只有显式 `stage` 才能写入获批的用户目录。
4. `suspected` 与 `excluded` 进程永不进入终止路径；一般 `owned` 必须同时匹配 launch ledger、PID 创建时间和 Job membership。唯一例外类别 `provisional-suspended-owned` 仅可在 A4b 已单独授权时成立：它必须是本次 Launcher 直接创建、从未 resume、原始 process handle 仍由本事务持有，且 PID、创建时间、launch nonce 全匹配的单个根 child；只允许终止该 child，不得扩展到后代、已 resume 进程或同名进程。一般 owned-tree 终止仍必须满足 Job membership 并走 A5。
5. 进程终止、配置修改、`.codex` 维护、安装器执行、提权/系统工具安装、证书/签名和外部发布分别使用独立授权门。
6. 阶段完成前不得创建对应 release 分支；release 分支只指向已验收 commit。
7. 每个阶段只存在一个活动 Goal；Goal 完成后必须交接到新 Codex 对话，旧对话停止继续修改。
8. 根仓库与受影响子仓库在 handoff 和验收时不得存在 staged change（已暂存改动）。
9. 不添加 remote，不 push，不 tag，不创建 PR；`ChatGPT-Fix` 仍是 `publishable: false`。
10. 原始 `.codex` JSONL、auth、token、cookie、完整环境变量和私密命令行不得写入仓库或普通日志。
11. `ChatGPT-Fix` 始终是独立 child repository（子仓库）；不得把它加入 root Git，不得创建 submodule、subtree 或镜像副本。

## 4. 目标结构

```text
ChatGPT Start Menu shortcut
  -> ChatGPT-Fix-Launcher.exe
      -> read current.json generation
      -> verify selected baseline state + manifest
      -> create launch ledger + Job Object
      -> launch isolated ChatGPT.exe
      -> observe owned descendants

ChatGPT-Fix-Manager.exe
  -> plan / activate / rollback / config-plan / maintenance-plan

ChatGPT-Fix-Packer.exe
  -> inspect / plan / stage / verify

%LOCALAPPDATA%\Programs\ChatGPT-Fix\
  manager\
  baselines\<package-full-name>\
  staging\
  runs\
  current.json
  previous.json
  logs\
```

`current.json` 和 `previous.json` 只保存 generation（代次）与相对 baseline ID，不接受任意绝对路径。所有由 state 驱动的文件操作必须执行 canonicalization（路径规范化）、containment（目录范围约束）和 reparse/junction 拒绝。

## 5. EXE 交付契约

| 工件 | 最小职责 | 明确禁止 |
|---|---|---|
| `ChatGPT-Fix-Packer.exe` | `inspect`、canonical `plan`、`stage`、`verify`；唯一生成 `chatgpt_fix.plan.v1` 与全量相对路径/大小/SHA-256 manifest | 修改 shortcut/current、启动 ChatGPT、终止进程、修改配置、清理状态 |
| `ChatGPT-Fix-Manager.exe` | 校验并展示 Packer/Core 生成的授权计划；事务化 `activate`/`rollback`；生成 config/maintenance proposal | 重复实现 package discovery/canonical plan、静默执行危险操作、绕过备份、充当常驻 watchdog |
| `ChatGPT-Fix-Launcher.exe` | 校验 current generation、启动 ChatGPT、维护 run ledger 和进程 ownership | 扫描并清理任意同名进程、修改 WindowsApps |
| `ChatGPT-Fix-Setup.exe` | P8 可选的 per-user（每用户）本地安装/卸载封装，只安装本项目二进制和快捷方式 | 内嵌官方包、运行旧安装器、提权写系统目录 |

首批 EXE 使用 Rust `cargo build --release --locked --target x86_64-pc-windows-msvc` 构建。P1-P7 的二进制只保存在 child task worktree 下未跟踪的 `out\<version>\win-x64\`，并在 receipt 中记录路径/hash；只有 P8 获得相应授权后，才复制到 `D:\project\release-packages\ChatGPT-Fix\<version>\win-x64\`。子仓库只跟踪 build receipt（构建回执）、SBOM、hash 和来源，不把官方包纳入 release bundle。

P1 必须保持真正离线：仅使用 Rust 标准库，设置 task-local `CARGO_HOME`/`CARGO_TARGET_DIR` 与 `CARGO_NET_OFFLINE=true`，所有 resolve/build 命令使用 `--offline --locked`，并在运行 Cargo 前证明 toolchain、MSVC target、rustfmt、clippy 均已安装。任何网络下载或用户级 Cargo/rustup cache 写入属于 A1n，缺失时失败关闭。

## 6. 阶段、Goal 与版本路线

| 阶段 | 单一 Goal | 主要产物 | 允许的最高权限 | 验收版本分支 |
|---|---|---|---|---|
| P0 | 冻结两书、Goal/branch/handoff 协议 | `计划书.md`、`执行书.md`、squad 本地状态窄范围 ignore | A0 仓库治理 | `release/0.2.0` |
| P1 | 建立 offline core（离线核心）、fixture-only dry-run 和可构建 EXE | Rust workspace；三个 EXE 的 `--version`/fixture `plan`；schema tests | A1 源码/依赖/build/fixture | `release/0.3.0` |
| P2 | 实现 live read-only discovery（实时只读发现） | Packer canonical `inspect/plan`；Manager plan-view；doctor v2 proposal（仅源码提案，不执行 doctor） | A2-P2 包/manifest/依赖/固定 shortcut 的阶段限定只读 | `release/0.4.0` |
| P3 | 实现 immutable baseline staging（不可变基线暂存） | Packer `stage`/`verify`；staging receipt；无 activation | A3 用户目录 staging 写入 | `release/0.5.0` |
| P4 | 实现事务化 activation、rollback 和具备最小 ownership 的 Launcher 启动 | current/previous generation；shortcut backup；launch ledger；PID 创建时间；Job 归属；smoke/rollback | A4a pointer/shortcut/启动；若使用 suspended 两步路径则另需 A4b 失败补偿 | `release/0.6.0` |
| P5 | 扩展 lifecycle ownership 观察闭环 | descendant membership、breakaway/reconciliation、退出报告；report-only | A4a 启动；不终止 | `release/0.7.0` |
| P6 | 验证 owned tree 的 graceful shutdown 与受控 Job close | owned-only shutdown policy；句柄继承/脱离负例 | A5 仅 owned 终止 | `release/0.8.0` |
| P7 | 验证 MCP/CODEX_HOME 隔离、按需启用和回滚 | effective-config diff；profile canary；config receipt | A6 配置写入 | `release/0.9.0` |
| P8 | 本地候选版硬化、maintenance dry-run、Setup/发布包与 native token-cost hardening | Packer/Manager/Launcher/可选 Setup EXE；SBOM；端到端回滚；native overlay reapply、helper health 与 repair receipt | maintenance dry-run 不消费 A7；A8a/A8b/A8c 仅对应可选动作分别确认；native overlay 写入/任务变更另设边界；无外部发布 | `release/1.0.0` |

`.codex` 的实际删除、vacuum 或 schema rewrite 不属于 1.0.0 必达项。若 P8 之后仍需实现，必须修订两书并建立新的 `release/1.1.0` 及独立 Goal。

### 6.1 Native token-cost integration 追加项

`NTC-NATIVE-20260801` 是用户明确授权接回本 ChatGPT-Fix 任务线的 native token-cost 集成。当前外部会话已报告完成 app.asar preload 注入、native-token-cost 资源写入、helper scheduled task 建立和重打包；这些事实只以 [records/native-token-cost-integration-20260801.md](records/native-token-cost-integration-20260801.md) 作为 delegated evidence 登记，不改变 P1 的 Rust 标准库、离线和不读取 live package/process/.codex 边界。

路由固定如下：P2 只设计离线 contract/proposal 和 launcher-owned overlay 的识别输入，不把该事项扩大为现有 A2-P2 的官方包 selector；P3/P4 把 overlay 作为不可变 launcher-owned baseline 层，记录 app.asar/resource before/after hash、backup、generation、rollback 和 update reapply；P8 才产品化 preload/resource/helper health/reapply/doctor/repair receipt 与 UI canary。当前 renderer overlay 没有 DOM/console 证明，不能把磁盘注入标记当作 UI 验收。

P2-P8 在当前执行书中只是路线入口摘要，不构成 `AUTHORIZED` 状态。为避免“无卡不能建 Goal、无 Goal 不能写卡”的循环，阶段 P<N> 在形成 candidate commit 前，必须在同一个 P<N> Goal 的 A0 治理收尾中把 P<N+1> 扩展为与 P0/P1 同粒度的详细执行卡，列出允许写集、禁止动作、命令、测试、失败补偿和授权消费，并取得门下规格 PASS 与尚书质量 PASS；不得另建规划 Goal。下一阶段所需授权也必须在 handoff 前明确取得，否则当前 Goal 保持 active、不创建 successor task。新任务仍只读 ACK，收到 `START_GOAL` 后才创建已经获批的单一阶段 Goal。

## 7. 授权门

| 门 | 授权范围 | 当前状态 |
|---|---|---|
| A0 | 治理内容：两书、窄范围 `.gitignore`、`VERSION`、root manifest 精确版本字段、本地 commits/branches/refs、`.repo-control` task/event 元数据、Goal、host task 与 `%TEMP%` handoff；不含产品源码 | 已由 2026-07-31 当前指令授予 |
| A0q | 将执行卡明确 allowlist 的非敏感 tracked 文档发送给 QoderCLI/Hermes 做只读审查/资料评估；每次先做 secret/private-ID scan，禁 session persistence/tools；辅助知识源为 `D:\project\.qoder\repowiki`（只读参考，不构成执行授权）；QoderCLI 可用 `Qwen3.8-Max` 与 Qoder DeepSeek 系列（`DeepSeek-V4-Flash`/`DeepSeek-V4-Pro`），Hermes 可用 `provider=deepseek` 的 DeepSeek 模型（本机默认 `deepseek-v4-flash`）；Qoder/Hermes 旁路尽量经 squad 工具编排 | 已由 2026-08-06 当前指令授予；不含 runtime/private 文件或未列名附件 |
| A1 | Rust 产品源码、fixture tests、本地离线构建 EXE、写入 task worktree 的构建输出/回执 | 已由当前“按两书执行并提供 packer/exe”指令授予；不含网络下载、用户级 cache 写入或系统安装 |
| A1n | Cargo/rustup/crate index 网络访问、下载 toolchain/target/component/crate 或写用户级 Cargo/rustup cache | 未授予；离线预检失败时单独请求 |
| A2 | `Get-AppxPackage`、live shortcut/process/config/state 只读探测及运行 `chatgpt-fix-doctor.ps1` | 未授予，P2 开始前确认 |
| A2-P2 | 仅在 P2 user-owned task 内，读取当前用户精确名称 `OpenAI.Codex` 的 Appx 注册对象、该对象的 `AppxManifest.xml`、manifest 声明的依赖包注册对象、manifest 指向的 primary EXE 元数据/SHA-256，以及 `%APPDATA%\Microsoft\Windows\Start Menu\Programs\ChatGPT.lnk` 的只读 COM 属性；允许 Packer 为此启动固定 `pwsh.exe -NoProfile -NonInteractive` probe | **已授予（P2 卡逐字确认）；不含** package 全量枚举、process/CIM/WMI/perf、`.codex`、Package `LocalState`、doctor 执行、网络、写入、启动 ChatGPT 或终止任何进程 |
| A3 | 写 `%LOCALAPPDATA%\Programs\ChatGPT-Fix\staging` 和 baseline | **已授予（2026-08-06 P2 收尾用户确认）；仅 Packer `stage`/`verify` 写 staging 与 immutable baseline；不含 pointer/shortcut/WindowsApps/config/启动/终止** |
| A4a | 修改 `current.json`、shortcut，实际启动隔离 ChatGPT；不含任何终止 | 未授予，P4 开始前确认 |
| A4b | 仅在 P4 启动事务失败时终止符合永久不变量第 4 条的单个 `provisional-suspended-owned` child | 未授予；若 P4 不证明创建时原子 Job 归属，则必须单独确认 |
| A5 | graceful shutdown、Job close 或终止任何 owned process | 未授予，P6 开始前确认 |
| A6 | 修改 MCP、`CODEX_HOME`、history 或其他有效配置 | 未授予，P7 开始前确认 |
| A7 | 归档、移动、压缩、删除、vacuum 或重写 `.codex` | 未授予 |
| A8a | 运行 Setup/安装器或卸载器 | 未授予，必须逐个工件和命令确认 |
| A8b | 提权或安装/修改系统级工具与依赖 | 未授予，必须逐个命令确认 |
| A8c | 访问证书、导入证书或执行代码签名 | 未授予，必须逐个证书/工件确认 |
| A9 | remote、push、tag、PR 或外部发布 | 未授予且项目不可发布 |
| A10-NTC | 接收并登记用户已授权的 native token-cost delegated evidence；P1 只写 receipt，不重复 live 写入、重打包、helper task 变更或重启 | 当前登记授权已给；后续 live 复核、overlay reapply、scheduled task mutation、安装/修复分别在对应阶段卡中逐项确认 |

每个 Goal 只能消费其阶段表中允许的授权。某阶段需要更高授权时，必须在新阶段对话中列出具体路径、命令、可逆性和验证方法后再请求，不得用“整体执行已授权”替代危险动作确认。

P2 只接受下列绑定式授权文本；其他同义许可不得自动扩大为广义 A2：

```text
AUTHORIZE_A2_P2_READONLY package=OpenAI.Codex publisher_id=2p2nqsd0c76g0 shortcut=%APPDATA%\Microsoft\Windows\Start Menu\Programs\ChatGPT.lnk exclude=process,.codex,LocalState,doctor
```

该授权只在 P2 task/Goal 存活期内允许重复执行执行书 P2 卡列出的相同只读 probe 与验收命令。它不授权运行既有 `scripts/chatgpt-fix-doctor.ps1`，也不授权读取 process、`.codex` 或 package `LocalState`。授权文本未精确匹配时，P1 Goal 保持 active，不创建 P2 successor task。

## 8. 分支、版本与 Goal 协议

- 工作分支固定为 `work/p<N>-<slug>`；本路线分别使用 `work/p0-plan-governance`、`work/p1-offline-core`、`work/p2-live-inspection`、`work/p3-baseline-packer`、`work/p4-activation-launcher`、`work/p5-lifecycle-observe`、`work/p6-lifecycle-enforce`、`work/p7-config-isolation`、`work/p8-release-hardening`。
- 阶段从上一 accepted release branch 创建工作分支；阶段验收后更新 `VERSION` 和 `workspace.yaml` 的 `version.current`，再建立对应 `release/<version>`。
- 根 `workspace.yaml` 若包含用户其他未提交更改，只能精确修改/审查 ChatGPT-Fix 的版本字段，不得暂存或提交无关 hunk。
- current-work technical ref 是本 task 的不可变 task-start identity：P1+ 必须保留并校验上一阶段预建 OID，绝不推进到本阶段新 governance OID；P0 若该 ref 尚不存在，只能创建到事务前 root preimage OID。仅 release/next-work refs 指向本阶段新 root governance OID。运行 `sync-branches` 前预览全部 child local branches 并要求每个 catalog ref 已存在，使该命令只验证 refs并写确定性的 `.repo-control/events/ChatGPT-Fix/branch-sync/` 事件。既有 ref 异 OID 立即停止；不得依赖 `sync-branches` 推进有效 ref。
- 每阶段开始只允许一个活动 Goal。下一任务可以先创建、materialize 并回报绑定 nonce/handoff hash/root ref+OID/child OID 的 `HANDOFF_ACCEPTED`，但在旧 Goal 标记 `complete` 之前不得调用 `create_goal`。旧任务用 host `read_thread` 验证 ACK 来自预期 task 后完成 Goal，再以幂等 `start_key` 发送 `START_GOAL`；新任务只创建一次下一阶段 Goal并回报 `GOAL_STARTED`。消息丢失时可重发同一 key，不得创建第二 Goal；除非用户明确要求，不设置 `token_budget`。
- 同一阶段内部允许多个小 commit，但只有 accepted commit 可以成为 release 分支头。

## 9. 新对话 handoff 协议

阶段完成时必须依次执行：

1. 在执行书形成本阶段 receipt 草案，并起草/审查下一阶段详细执行卡；下一卡与所需授权均 PASS 后，运行本阶段测试、`git diff --check`、secret/placeholder scan、根与子 index 检查并完成两级审查。
2. 更新 `VERSION`，先形成不承载 release ref 的 child candidate commit；该 commit 不是 accepted release。
3. 在 canonical root 仅暂存 ChatGPT-Fix `version.current` 的精确 hunk，复核 staged diff 后形成 bounded root governance commit；不得纳入用户其他 root 改动，并捕获其 OID。
4. 把 root governance OID 和最终证据写入 receipt，形成 child accepted commit。若此步失败，必须用新的 bounded root rollback commit 恢复旧版本，不能留下 manifest drift。
5. 从 accepted commit 创建 child release branch与下一阶段 child work branch；确认 canonical child 的 tracked worktree/index 完全干净后对齐 release。不得在 canonical checkout 切换到 next-work。
6. current-work ref 只校验 task-start OID（P0 缺失时创建到 root preimage）；仅 release/next-work refs 创建或校验到新 root governance OID。随后预览全部 child branch/catalog ref 与将写入的 branch-sync event，再运行 `sync-branches` 和 `repo-control doctor`。既有异 OID停止，sync 输出不得出现意外 `created` ref。
7. 在 `%TEMP%` 按 `handoff` skill 生成精简 handoff，只引用两书、commit 和回执，不复制全文；生成随机 handoff nonce 与文件 SHA-256，冻结文件后不得再改写。
8. 先用 `list_projects` 取得 saved project `D:\project` 的实际 `projectId`，再用 `list_threads` 检查是否已有同 branch 的兼容任务。下一任务由用户 2026-07-31 已明确要求的 Codex host `create_thread` 生命周期动作创建，是 user-owned task，不是旧 root 对新 root 的 court 派遣；没有这项用户/host 授权时必须停止请求用户。
9. host 创建请求必须包含 project worktree `startingState.branchName`、完整 prompt、source thread ID、handoff 路径/nonce/SHA-256、accepted child OID 与 root ref/OID。新任务执行 `materialize-current --root-branch <same-ref> --task <unique-id> --attach-current`，验证 task shell 中的 `workspace.yaml` 版本、`attached\ChatGPT-Fix`、根/子 index与所有绑定值。
10. 新任务通过 `send_message_to_thread(source_thread_id)` 回传包含 nonce、handoff SHA-256、root ref/OID、child OID 的 `HANDOFF_ACCEPTED` 并等待，不创建 Goal。旧任务必须对 host 返回的预期 thread ID 调用 `read_thread`，验证 ACK 与 mount/state；若 host 只先返回 `clientThreadId`，使用 `list_threads` 解析最终 thread ID。超时只等待/复查，不重复创建。
11. 旧任务验证 ACK 后标记当前 Goal `complete`，计算不可变 `start_key` 并发送 `START_GOAL`。若发送或回执丢失，旧任务只可重发同一消息，不再修改文件/refs；新任务对重复 key 幂等处理，只创建一次 Goal并回报 `GOAL_STARTED <start_key>`。收到该回执后旧任务停止协调。真实 task ID 只保存在 host 状态/临时参数，不为记录 post-release ID 移动 release branch。

## 10. Court 派遣与 QoderCLI/squad 旁路

每个 visible phase task（可见阶段任务）是 Codex host 按用户明确指令创建的 user-owned root/太子协调 shell，不是旧 root 派出的 agent，也不是某个六部办公室。上一任务只能准备并验证 host 创建请求；receipt 将 authority 记为 `user standing instruction -> Codex host`，不得记成 `root -> root`。新任务内部的 lifecycle operation（spawn/reuse/follow-up/wake/restart/recovery/reassignment）固定使用以下 court 路径：

| 工作类型 | 合法路径 |
|---|---|
| 计划拆分与资料归纳 | root -> 中书省 |
| 实现与构建 | root -> 尚书省 -> 对应六部 -> 该部属司/作坊 |
| 规格验收与驳回 | root -> 门下省 |
| 新阶段 user-owned task 创建 | 中书省审交接方案、门下省确认验收条件；随后由用户已授权的 Codex host 执行 `create_thread`，court dispatcher 记为 none |

root 不得直接派遣六部或其下属；尚书不得越级调用其他部门下属。host 创建 user-owned task 的授权不扩大任何 court 派遣权限。发生 direct-superior violation（直接上级违规）时，输出立即 quarantine（隔离），不得进入 commit/release，必须经正确上级重新审查和接受。阶段 receipt 必须记录 court role、direct superior、dispatch path 和被隔离结果。

QoderCLI、`squad` 与 Hermes 只作为可选 A0q read-only advisory lane（只读咨询旁路），适用于审查、资料搜集和评估：

- 默认调用采用 `qodercli --print --no-session-persistence --tools "" --attachment ...`，附件必须由当前详细执行卡逐项列名；不授予文件工具、Git、shell 或任何 A1/A1n-A9 权限。
- 允许的辅助模型：QoderCLI 使用 `Qwen3.8-Max` 与 Qoder DeepSeek 系列（`DeepSeek-V4-Flash`/`DeepSeek-V4-Pro`）；Hermes 使用 `provider=deepseek` 的 DeepSeek 模型（本机默认 `deepseek-v4-flash`，非交互形态 `hermes -z "<prompt>" -m <model> --provider deepseek --cli`）。
- 辅助知识源：`D:\project\.qoder\repowiki`（RepoWiki 知识库）仅供只读检索/参考；引用仍以 tracked 文档与 court 审查为准，wiki 内容不构成执行授权。
- Qoder/Hermes 旁路尽量通过 `squad` 工具登记/编排；`squad` 只登记/编排旁路任务；旁路成员不得创建 Goal、推进阶段状态机、创建 Codex task 或修改文件。
- Qoder/Hermes 结论是 advisory evidence（咨询证据），不能覆盖门下规格审查；采纳前必须由合法 court 路径复核。
- 每次调用前先对附件做 secret/private-ID scan；receipt 只记录 QoderCLI/Hermes 版本、限制参数、不可逆短标签、结论和是否采纳，真实 thread/task UUID 仅可短期保存在 `%TEMP%` handoff，不写入 Git 或普通日志。
- squad 本地状态只允许落在被忽略的 `.squad/` 与 `locks/*.receive.lock`；不得提交消息数据库、session、role 或 receive lock。若 `squad init` 自动改写代理说明文件，必须在验收前撤回。

## 11. 验收矩阵

| 维度 | 必须证明 |
|---|---|
| 文档与治理 | 两书无冲突；Goal/分支/版本/handoff 可按命令执行；历史文档不再产生执行授权 |
| Dry-run | 相同输入输出确定；stdout-only；无状态写入；逐项列出 would-copy/write/switch/start/terminate |
| Package | 候选包身份、架构、publisher、manifest、依赖与前后快照稳定；并发更新时失败关闭 |
| Staging | 源/目标全量 manifest 一致；磁盘满、取消、hash mismatch、长路径、reparse point 均有测试 |
| Activation | pointer、shortcut、baseline 三层备份和回滚；失败只撤销本事务，不删除旧基线 |
| Lifecycle | `owned/suspected/excluded` 可解释；外部 Python/Hermes/Git、PID 重用、handle inheritance 为负例 |
| Config | effective config 已证明；配置 diff、备份和回滚一一对应；不复制 auth/token |
| Artifacts | EXE hash、toolchain、Cargo.lock、SBOM、签名状态与 build receipt 完整；不含官方包 |
| Workspace | child release 分支、root technical ref、`VERSION`、`workspace.yaml` 一致；root/child index 为空 |

## 12. 风险登记册

| 风险 | 处置 |
|---|---|
| 隔离复制失去 MSIX 身份/依赖能力 | P2 资格检测，P3 staging，P4 smoke 前不切 shortcut；失败保留 legacy baseline |
| 复制期间官方包更新 | 复制前后包身份/manifest/hash 快照；变化即中止并隔离 staging |
| 同名进程误杀 | 名称/年龄只进入 suspected；终止路径必须验证 ledger、PID 创建时间和 Job membership |
| Job Object 与 Electron breakaway/嵌套作业冲突 | P4 详细卡必须二选一：证明创建时原子 Job 归属不会产生未归属 child，或先取得 A4b 后采用 suspended 两步路径并仅补偿从未 resume 的精确 child；P5 扩展观察，P6 才允许一般 owned-tree 动作性 canary |
| `CODEX_HOME` 导致登录、配置、会话隔离 | P7 使用空 canary profile，不复制 auth；effective-config 与功能验证后才提案迁移 |
| unsigned EXE 信任不足 | 每个工件记录 SHA-256、toolchain、源码 commit、SBOM、签名状态；证书/签名属于 A8c |
| Qoder 附件数据出域 | 仅 A0q allowlist 的非敏感 tracked 文档；调用前 secret/private-ID scan；禁 tools/session persistence；任何偏差进入 receipt |
| ACK 或 `START_GOAL` 丢失/重放 | nonce+handoff hash+root/child OID 绑定 ACK；host `read_thread` 验证预期 task；`start_key` 幂等重发并要求 `GOAL_STARTED` |
| 根 workspace 有用户脏改动 | 精确 hunk 修改，不重置、不清理、不暂存无关内容；handoff 明确记录残留 |
| native token-cost app.asar overlay 随 baseline/app 更新漂移 | 每次 reapply 绑定 source/resource manifest、app.asar before/after hash、backup、generation 与 rollback；hash 或 marker 不匹配时失败关闭 |
| helper Scheduled Task 漂移或 UI overlay 未实际渲染 | helper health 与 task definition 分开验收；磁盘 marker 只证明静态注入，必须另做保留用户会话的 UI canary |
| 版本过度膨胀 | 只有 P0-P8 大阶段产生版本；阶段内修订只使用工作分支 commit |

## 13. 术语表

| 英文术语 | 中文注释 |
|---|---|
| baseline | 从本机注册官方包生成的隔离基线副本 |
| staging | 尚未激活、可隔离验证的暂存目录 |
| Packer | 发现、规划、复制、验证 baseline 的非启动型工具 |
| Launcher/Supervisor | 稳定启动入口与应用存活期监督器 |
| launch ledger | 记录 launch ID、generation、PID 和创建时间的启动账本 |
| Job Object | Windows 用于约束和查询进程树归属的作业对象 |
| provisional-suspended-owned | A4b 唯一例外：本事务直接创建、从未 resume、原始 handle/PID/创建时间/nonce 全匹配但尚未取得 Job membership 的单个根 child |
| dry-run | 只输出计划而不改变状态的演练 |
| receipt | 可审计的输入、动作、结果和 hash 回执 |
| fail closed | 无法证明安全时停止，不猜测继续 |

## 14. 修订记录与计划变更规则

| 修订 | 日期 | 变更 | 审查状态 |
|---|---|---|---|
| rev-1 | 2026-07-31 | 建立 P0-P8、Goal/版本/handoff、EXE 和授权基线 | P0 审查中 |
| rev-2 | 2026-07-31 | 补齐 task worktree、root governance commit、Goal 串行、Court 派遣及 QoderCLI/squad 旁路 | P0 规格复审中 |
| rev-3 | 2026-07-31 | 补齐 P0 路径、candidate/accepted/root-ref 事务、host-owned task 与 ACK 可重入流程 | P0 规格复审中 |
| rev-4 | 2026-07-31 | 加固 fail-closed 原生命令、精确 manifest hunk、原子 refs、私有 ID、独立 A8 门及首次启动 ownership | P0 质量复审中 |
| rev-5 | 2026-07-31 | 消除下一阶段卡/Goal 循环；拆分 A4a 启动与 A4b 未 resume child 失败补偿；实测精确 hunk dry-check | P0 规格回归中 |
| rev-6 | 2026-07-31 | 定义 A4b 唯一 `provisional-suspended-owned` 例外并保持一般 owned-tree 的 Job membership/A5 不变量 | 门下最终规格 PASS；尚书技术项 PASS |
| rev-7 | 2026-07-31 | 收窄 A0/A1并新增 A0q/A1n；固定 P1 Cargo offline；绑定 ACK/start_key；显式 repo-control 元数据与恢复 journal | P0 双审回归中 |
| rev-8 | 2026-07-31 | 固定 current-work root ref 为不可变 task-start/preimage OID；仅 release/next-work 指向新 governance OID | P0 规格回归中 |
| rev-9 | 2026-07-31 | 将 atomic journal 接入每个事务断点；绑定 expected task-start OID；验证 branch-sync event 路径与内容 | 门下规格 PASS；尚书质量 PASS |
| rev-10 | 2026-08-01 | 将 P2 扩展为阶段级详细卡；新增 A2-P2 固定 package/manifest/dependency/shortcut 只读子门，并明确排除 process、`.codex`、LocalState 与 doctor 执行 | P1 收尾规格/质量审查中；A2-P2 未授予 |
| rev-11 | 2026-08-06 | 扩展 A0q：纳入 `D:\project\.qoder\repowiki` 知识库（只读参考）；QoderCLI 允许 `Qwen3.8-Max` 与 Qoder DeepSeek 系列模型；新增 Hermes DeepSeek 只读旁路；Qoder/Hermes 尽量经 squad 编排；仍禁 tools/session persistence/文件修改 | 门下规格 PASS；尚书质量 PASS（2026-08-06 恢复轮 serial_inline 复核，Qoder advisory 三项建议已处理） |

阶段可以更新执行书中的实时状态和回执；改变永久安全不变量、阶段范围、版本路线、EXE 职责或授权门时，必须同时更新本计划书的修订记录，先完成规格审查，再继续当前 Goal。任何阶段不得以“实现已开始”为理由绕过计划变更审查。
